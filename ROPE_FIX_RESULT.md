# 第二个端侧 bug：rope_base 10000 → 1e6（已修，真机行为剧变）

## 1. 定位过程（纯 FP16，无 QNN）

规范对拍脚本 `scripts/f0_ab_canonical.py`（逐位置连续推进，单一函数构造全部微图输入）：

```
pos | cache | logits cos | hidden cos | kdelta0 cos
  1 | post  | 0.990895   | 0.991894   | 0.999874
  4 | post  | 0.992245   | 0.992520   | 0.998046
 16 | post  | 0.988990   | 0.991125   | 0.972379
 32 | post  | 0.803785   | 0.843234   | 0.922422
 64 | post  | 0.513298   | 0.694694   | 0.796369
131 | post  | -0.025814  | 0.213795   | 0.431188
```

- pos≤16 已基本 PASS（~0.99）⇒ **之前的 0.85 是旧脚本 skip 位置造成的伪影，已消除**
- 关键：**`key_delta_0` 是当前 token 的 K，完全不依赖 cache，却从 0.9999 衰减到 0.4312**
  ⇒ 只能是 **RoPE 频率本身不对**（相位误差随位置放大）

## 2. base 扫描（决定性）

```
pos | b=10000      | b=1e6        | b=500000     | b=100000
 16 | 0.9890/0.9724| 0.9926/1.0000| 0.9924/0.9997| 0.9916/0.9961
 64 | 0.5133/0.7964| 0.4129/0.9999| 0.5344/0.9959| 0.4158/0.9619
128 | 0.3783/0.4469| 0.9876/1.0000| 0.3435/0.9861| 0.4043/0.9218
131 |-0.0258/0.4312| 0.9972/1.0000| 0.0262/0.9859|-0.1080/0.9193
   格式 = logits cos / key_delta_0 cos
```

**`rope_base = 1e6` 时 `key_delta_0 cos = 1.0000`（全位置）**，`logits cos @pos131 = 0.9972`。
=> 官方 Audio8 用 **Qwen 风格 1e6**。

host 原代码：`float ropeBase = 10000.0f;  // verify from config` ——
注释写着待核实，而官方 `config.json` 是 3 字节空文件，所以从未核实过。

**已修**：`p1-runner/audio8_engine.h` → `1000000.0f`，证据写进注释。

## 3. 真机效果（两处 host bug 都修完后）

```
                修复前          buildBank 修复后      + rope 修复后
帧数            27              27                    64（跑满上限）
step0 margin    0.0381          0.0270                0.1288
step1 margin    —               0.4101                2.0627
step2 margin    —               0.4440                0.6310
塌缩            无               step≈20 起固定点循环  前 24 步无固定点
PCM peak        0.4714          0.1819                0.1033
```

TRACE 前 24 步 semantic：`2091 501 3519 1591 2905 152 534 534 131 131 35 35 35 131 35 35 35 131 131 131 131 131 131 131`
（仍有重复，但已不是同一个 cb 十元组无限循环）

## 4. 仍未达成

```
官方 golden 59 帧 vs 手机 64 帧：逐帧完全一致 0/59
官方 cb0[:12] = [3571, 2323, 3703, 3331, 2701, 1263, 162, 72, 262, 852, 466, 317]
手机 cb0[:12] = [2091, 501, 3519, 1591, 2905, 152, 534, 534, 131, 131, 35, 35]
cb1..cb9 相同比例 0.000 ~ 0.017
```

但这次 **B(FP16 微图) 已经与 A(官方) 对齐到 0.9972**（pos131），
所以手机与官方的差距现在**可以干净地归因于 F1：QNN A16W8 量化/执行**。
这一步以前做不了（因为 B 自己就是错的），现在可以做了。

## 5. 状态表

```
F0-A    官方 FP16 full chain                  ✅ GOLDEN
F0-B0   position=0 微图结构一致性             ✅ 0.983/0.990
F0-B1  cache layout (buildBank 轴序)          ❌ BUG FOUND → ✅ FIXED → 真机行为已变
F0-B2  residual cache semantics               ✅ RESOLVED（根因是 rope base，不是 cache）
        - bf16 rope                            ✅ 排除
        - pre/post-RoPE cache                  ✅ 排除（pre 不更好）
        - rope base 10000 -> 1e6               ❌ BUG FOUND → ✅ FIXED
F0-B3  A↔B 最终对齐                            ✅ pos131 logits 0.9972 / kdelta 1.0000
F0-C   Fast 微图同法对拍                       ⏭ 下一步（rope 已同时修复，Fast 也受影响）
F1     B(FP16 微图) ↔ C(手机 A16W8 QNN)        ⏭ 现在才第一次变得可测
F2     重转 QNN / calibration                  仅当 F1 FAIL
V2     新 Slow 设计                           冻结
```

## 6. 请试听（工作区 listen-kit-20260915/）

| 文件 | 内容 |
|---|---|
| **P_ropeFix_phoneCodes_officialCodec.wav** | rope 修复后手机 AR codes，走**官方 FP16 codec** ⇒ 单独评判 AR |
| **Q_ropeFix_deviceQNNcodec.wav** | 同一批 codes 走端侧 QNN codec ⇒ 与 P 对比可分离 codec 问题 |
| GOLDEN_fp16_reference.wav | 官方基准（你已确认可懂） |

数值（不能替代试听）：

```
P: n=131072 rms=0.02035 peak=0.09077 acf=[0.9948,0.298,0.3144,0.2135,0.0793] flat=0.0331 zcr=0.0244
Q: n=131072 rms=0.02764 peak=0.10331 acf=[0.9951,0.2543,0.4624,0.3329,0.0931] flat=0.0776 zcr=0.0227
```

P 的 rms/peak 明显偏低（0.020/0.091），如果听起来是「很轻的嗡鸣/听不清」，
说明 AR 仍未收敛到正常语音；如果已能听出字，则说明两个 host bug 就是主根因、
剩下的只是 QNN 量化精度（F1/F2）。
