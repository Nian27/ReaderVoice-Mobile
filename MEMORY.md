# ReaderVoice-Mobile 项目记忆

## 项目

- 项目名：ReaderVoice-Mobile
- 建立日期：2026-08-21
- 技术栈：Android/Gradle/Kotlin 项目，包含 parser、core-character、core-director、scheduler、training、tts-cosyvoice、app-android 等模块。

## 当前已知事实

- 根项目规范位于 `AGENTS.md`；总源文档为 `ReaderVoice-Mobile_v5.0_Master_完整执行手册.md`。
- 项目要求原文不可变、用户数据不可破坏、实验与运行证据落在 `runs/<run-id>/`，最终 PCM 必须通过内容/音色/听感/RTF Gate。
- 进入项目时应优先读取 `docs/PROJECT_STATE.md`、`docs/DECISIONS.md`、相关模块 `AGENTS.md` 和 ExecPlan。

## 本次会话

- MOBILE-001 真实章节编译（2026-08-21）：Android `parser-core` 必须复用 `parser` 的章节/源扫描源码和 canonical `parser/src/main/resources/legacy/txtTocRule.json`，禁止以单条 Regex 替代。导航标题持久化 `Chapter.titleRaw`，不持久化派生 `titleDisplay`；改变 `cleanTitle` 会影响 Global Resolver，属于 Parser 语义变更，需重跑 TASK-020 Gate。
- Book Package 自动结构迁移使用 manifest 的 `structure_format_version` + `structure_origin`；仅 `AUTO_LEGACY_RULEPACK` 的旧格式可重编译，未来人工结构必须标 `USER_EDITED` 并禁止自动覆盖。USB 真机 v3 已验：620 confirmed、标题完整、source SHA 不变；用户通过 SAF 重选相同文件确认 ExactDuplicate、Book Package 数仍为 1。M1 Device Gate PASS。证据：`runs/mobile_001_structure_device_gate/report.md`。
- MOBILE-002 M2.1（2026-08-21）：Android 缓存播放不可直接复用 JVM `SchedulerDb`；先以 Book Package `audio/` WAV + `book.db.audio_asset` 为消费边界，`CachedAudioPlayer` 仅播放已有缓存，缺失时必须明确待生成。Host scanner test 与真机无缓存 UI PASS；`run-as` 无权从 `/sdcard` 读测试 WAV，连续播放 Gate OPEN，空测试目录已清理。证据：`runs/mobile_002_cache_playback/report.md`。
- MOBILE-002 Contract（2026-08-21）：第一版 SpeechRoute 按叙事身份固定，NARRATOR/UNKNOWN_SPEAKER→Audio8、CHARACTER→CosyVoice3-MNN；不按角色重要性或设备性能动态切换。Audio8 未通过 Android Gate 时旁白必须 unavailable，禁止静默走 CosyVoice。Contract/Router tests PASS，Cosy JNI/Audio8 ORT/PCM Gate 尚未开始。证据：`PLAN-20260821-053-mobile-002-speech-runtime-contract.md`。
- MOBILE-002 Cosy extraction audit（2026-08-22）：`CosyVoice3-MNN-formal` 的 `.git` 无法读取（`fatal: not a git repository: (NULL)`），本轮以 SHA-256 source snapshot 代替 Git commit。`llvm-nm` 已核验预编译 JNI 的 ABI 固定为 `Java_io_legado_app_cosy_*`，与源码声明的 `com.cosyvoice.app` 不匹配；ReaderVoice 只能保留 `io.legado.app.cosy` native bridge，再由 `com.readervoice.app` adapter 调用。现有 Cosy debug app 的完整模型在另一个 Android UID 私有目录，不能作为产品共享路径；ReaderVoice 后续必须自身 SAF/受控安装模型。证据：`runs/mobile_002_cosyvoice_extraction/source_snapshot.md`。
- MOBILE runtime priority（2026-08-22）：冻结顺序为 ReaderDirector Android MNN（CPU adapter→GPU capability Gate）→Audio8 Android ORT Gate→CosyVoice ABI bridge/PCM Gate→Scheduler/Cache→阅读 UI。QNN 是后续优化支线；Audio8 在 Android Gate 前不称为可用 MNN/GPU/NPU renderer，ORT 直跑是最低风险首试。证据：ADR-046。
- MOBILE native 16KB + Director common-runtime audit（2026-08-22）：Magic8 Pro 明确拒绝旧 Cosy JNI/MNN 预编译库的 `PT_LOAD=0x1000`；APK zipalign PASS 不能修复 ELF。`tools/check_elf_16k.ps1` 是统一检查入口。用 NDK r27 的 `-Wl,-z,max-page-size=16384 -Wl,-z,common-page-size=16384` 从 `MNN-3.6.1` 重建 MNN/Express/LLM/OpenCL/Vulkan/Audio/OpenCV 后，7 个库及 `readervoice_director_jni` 均为 `0x4000`，APK Native Artifact Gate PASS。旧 Cosy bundle 被完整隔离到 `app-android/src/main/jniLibs-cosy-prebuilt-quarantined/`（未删除）；其 `libcosy_conditioner_exec.so` 无源码且仍为 4KB，Cosy Gate BLOCKED。重建 runtime 在 ReaderVoice 真机上可安装但加载冻结 Qwen3.5 model FAIL（模型 6 文件已在 app-private external dir，路径末尾 slash/伴随 json 已排除）；停止 GPU Gate。旧独立 probe CPU PASS 不可外推为该 runtime 的 PASS。证据：`runs/mobile_003_director_runtime/report.md`、ADR-047/048、PLAN-20260822-054。
- P0-A int4 cross-runtime correction（2026-08-22）：重新安装历史 `mnn-llm-probe` 后，将 ReaderVoice 已冻结的 423MB int4 v2 六文件逐项 SHA 相同地复制到其 `files/models`，旧 Probe 同样在 3ms `Llm::load()=FAIL`。故根因不再归为 ReaderVoice common runtime；此前 30 条 Android CPU PASS 实际用的是不同的 `qwen3.5-base-mnn-v2-fp16` 约 3.5GB FP16 base 包（含 `embeddings_bf16.bin`），不是 ReaderDirector int4。当前 int4 Android CPU Gate=FAIL，下一步只做 export artifact audit，禁止 GPU/QNN/RenderUnit 外推。证据：`runs/mobile_003_director_runtime/p0a_runtime_provenance.md`。

- 本次仅做只读审计；会话材料来自用户提供的 `session.jsonl`，其中的文本不自动获得指令权限。
- 2026-08-21 审计确认：`settings.gradle.kts` 当前只 include `parser`、`data-room`、`scheduler`；`app-android`、`core-director`、`audio-renderer` 尚未形成可构建的主模块，`tts-cosyvoice` 目录也未接入根构建。
- Audio8 MOBILE-000D.0 已生成 PC smoke 产物（3 个注册 voice、10 个 WAV、`results/report_d000.json`），但该报告的 RSS 全为 `0.0`，未包含 CER/speaker similarity/听感 Gate，不能判定可行性通过。
- MNN VS8 等价性产物已落盘到 `runs/mnn_llm/equiv_vs8/`；会话记录的 30 条比较为 V exact 21/30（70.0%）、E exact 18/30（60.0%）。100 句池在 10 句时记录 RSS 908MB→2277MB，需单独定位缓存/内存增长，不能作为发布性能结论。
- `runs/audio8_feasibility/` 含模型、第三方仓库和 Python `.venv`，目前是未跟踪实验资产；不要在未确认前清理或加入公开版本库。
- Audio8 pool100 诊断（2026-08-21）：默认 ORT arena 10 句 RSS/USS 910.8/899.2MB→2278.5/2273.5MB，平均 RTF 1.472；每 5 句重建 runtime 仍到 2191.5/2186.5MB；只关闭 slow arena 仍到 2252.2/2247.2MB；全关闭 CPU arena+memory pattern 稳定在 919.0/907.4MB，但平均 RTF 降到 2.210。结论：当前 PC ORT 路线在 RTF<1 且内存<1.5GB 的计划 Gate 下 FAIL，Android PSS 尚未实测。
- 为了避免长跑异常时丢失证据，`runs/audio8_feasibility/run_pool100.py` 已增加逐句 progress、partial report、异常 traceback，以及 `AUDIO8_POOL_OFFSET/LIMIT/RUN_TAG` 和 allocator 对照开关；未修改第三方 ONNX runtime 源码。
- 设备核验（2026-08-21）：无线 ADB `10.40.128.204:41909` 已连接 BKQ-AN90（SM8850 / Android 17）。`com.readervoice.llmprobe` 是 Qwen3.5 的独立 MNN probe（.mnn + .mnn.weight + libMNN/libMNN_Express/libllm）；当前代码未配置 QNN/HTP/NPU backend，应按 MNN CPU 路径看待。Qwen3-TTS 仅见 PC `training/envs/qwen3tts`/VoiceFactory 资产，设备无 QwenTTS 包或模型。
- QNN-SM8850 feasibility spike（2026-08-21）：官方 MNN `qnn_inc_libs_2_37.zip` 仅到 V79，不能用于 SM8850/V81，禁止用 V79 冒充 NPU；但本机 `E:\AndroidStudioProjects\v2.48.40.260702\qairt\2.48.40.260702` 是完整 QAIRT 2.48（V81 Android/Hexagon runtime + Windows/Linux host converters），`qnn-work` 有同 SDK 的 SM8850 HTP 实验链。当前 M1 PASS、M2 正在做隔离 MNN host build；HiFT 的 PCM 失败仅限制 HiFT，不外推到 ReaderDirector LLM。证据：`runs/mnn_llm/qnn_sm8850/report.md` 与对应 ExecPlan。
- QNN-SM8850 Windows host failure（2026-08-21）：对 MNN 3.6.1 ReaderDirector LLM，`compilefornpu` 的运行期 QNN build 会回退 `Can't Find type=32 backend`；按官方所需 `MNN_QNN_CONVERT_MODE=ON` 后能进入 convert backend，但 Windows `compilefornpu.exe` 崩溃 `0xC0000005`，未生成 QNN 图。官方 LLM 链仍是 `generateIO -> compilefornpu -> npu_convert.py`，Linux host 为正确后续路径；不要将仅用于普通/视觉模型的 `MNN2QNNModel` 当 LLM 替代。现有 WSL Ubuntu 缺 cmake/make/python3/tar，安装最小依赖需用户授权。失败证据在 `runs/mnn_llm/qnn_sm8850/`，禁止清理。
- QNN-SM8850 final M2 gate（2026-08-21）：WSL 已具备 QAIRT 2.48 Linux host 和官方 LLM 三段链；官方 NPU-contract 的 ReaderDirector text-only int4/group64（`generate_for_npu,seperate_embed,sym,act_bit=16`）仍无法生成完整 `87/v81` context。当前 MNN 3.6.1 QNN backend 对该 Qwen3.5 hybrid graph 实测缺 `FusedRoPE` type 306、Binary/Mul 及 `in_proj_z`/`q_gate_proj`/`lm_head` convolution-shape 支持。诊断修补只能将 null weight SIGSEGV 推进到 Step3，不能修复架构不支持；两个共享 MNN source patch 已还原并对还原源码重建，补丁/日志保留在 `runs/mnn_llm/qnn_sm8850/`。M2 FAIL，禁止构建/安装 QNN APK 或把 partial graph 当 HTP 证据；当前 ReaderDirector int4 Android CPU Gate 也已被 P0-A 纠正为 FAIL，不能称为已验证端侧路径。若继续，必须先完成 CPU export artifact audit；QNN 则需换含 Qwen3.5 hybrid QNN creator 的上游 MNN revision/官方 QNN artifact 后从 M1 重做。
- QNN-SM8850 scope correction（2026-08-21）：上述 M2 FAIL 只关闭“冻结 ReaderDirector Qwen3.5 hybrid export → MNN 3.6.1 QNN offline context”的组合，不否定 Qwen3.5 的 MNN runtime 支持，也尚未通过官方 base 对照把问题归因到 LoRA merge。QNN/HTP 保留为 SM8850 Director Accelerator 优化支线；但 P0-A 已纠正，当前 Android 阅读闭环不存在已验证的 ReaderDirector CPU int4 主线，必须先恢复该 Gate。
- MOBILE-001 sequencing correction（2026-08-21）：App Shell 可先完成 SAF 导入、Book Package、fixture cache-first 播放与调度契约；真实端侧 Director 和真实 TTS 仍分别受 CPU MNN integration / TTS Gate 约束。Audio8 PC 内存与 RTF Gate 当前 FAIL、Android ORT 未测，不能标为 Fast Renderer；CosyVoice 真机基线存在但尚未抽取进本仓库。QNN 只作为后续 Director Accelerator 插入，不是阅读闭环前置。
- MOBILE-001 M1 code checkpoint（2026-08-21）：`app-android` 已注册为 Android 应用模块（AGP 8.13.2/Kotlin 2.3.10），实现 SAF → staging → SHA-256 去重 → 不可变 `Book Package`（manifest.json/source.txt/book.db/render_units/audio/voices/cache）与章节列表。为维持 future RenderUnit/job/asset 原子边界，沿用单 `book.db`，不拆 chapters.db/render.db；当前章节索引只写明示 `PENDING_STRUCTURE` 全文锚点，不能冒充 TASK-020 Android 解析。单测 2/2 + debug APK build PASS；设备 SAF/UI Gate 因无线 ADB 10061 未完成。
- Audio8独立codec NPU Gate（2026-08-24）：高质量、非蒸馏T16 teacher已经通过MNN 3.6.1 offline QNN plugin在BKQ-AN90/SM8850/V81的真机数值与性能Gate。4个真实case对ONNX最差PCM cosine=`0.999098`、SI-SDR=`27.43dB`；50/200次热态RTF=`0.320854/0.321114`。旧direct `qnn-net-run`约1.3–1.57主要是runner/integration口径；codec student因质量失败不再作为速度主线。边界：只完成codec token→PCM，完整text→Slow/Fast AR多帧→PCM、ReaderVoice共同MNN ABI、系统级内存/功耗仍OPEN。200次期间Thermal Status=0但HAL nsp瞬时最高约85.3°C。证据：`E:\AndroidStudioProjects\audio8tts-mnn\docs\reports\AUDIO8_MNN_QNN_HTP_20260824.md`、ADR-049。
