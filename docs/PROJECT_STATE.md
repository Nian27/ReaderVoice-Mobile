# PROJECT_STATE.md — 当前冻结事实

**更新：2026-08-20（TASK-095-VS8 FROZEN）** | 只放冻结事实，不写日记。版本/结论变更必须先过 Gate 再改本文件。

## 当前架构与总源

- Master：`ReaderVoice-Mobile_v5.0_Master_完整执行手册.md`（v5，2026-08-12）。文档优先级：实际代码/测试 > 本文件 > DECISIONS > Master Manual > ExecPlan。
- 里程碑路线：M0 治理 → M1 TXT Source → M2 Chapter/Paragraph → M3 DB/Revision → M4 Character → M5 v90.7 Migration → M6 Director Baseline → M7 0.8B LoRA → M8 Teacher Distill → M9 Hard Mining → M10 2B Upper → M11 MNN Director → M12 CosyVoice Engine → M13 Enrollment → M14 Control Matrix → M15 RenderPlanner → M16 Playback Bootstrap → M17 Scheduler → M18 Multi-book → M19 Final 8GB Gate。

## 三条基线（2026-08-12 冻结）

### 1. Chapter Regex 基线
- 源：`FUN-legado/app/src/main/assets/defaultData/txtTocRule.json`（26 条默认规则，family 齐全），执行参考 `TextFile.kt:getChapterList`。
- 用法：Structural Compiler 的 `LegacyChapterRulePack` 初始规则，regex 只产候选。

### 2. 多角色 v90.7 基线（mingwuyan_v907）
```text
Legacy multi-role baseline: mingwuyan_v907
Source type: Legado TTS rule JSON（code 字段内嵌 JS）
Version: 907 | source: tts-rule-2.85-v907 | author: 命無言、萌新、M
JSON ≈ 1.94MB / 28,564 lines | code ≈ 779,255 chars / 14,058 JS lines
SHA-256: 35c7d654274fa0f0ad8bda0d6498e23789a2f2f053cdbd8ec8eb48f02759842e
Status: Verified available（research/third_party/legado-v907/，RESEARCH_ONLY）
Usage: Requirements reference / Behavior reference / Hard-case & data-mining source
Not allowed: Direct shipping dependency / Remote upload migration /
            Commercial redistribution until permission clarified
```
- 逆向结论：12 能力域 → 13 迁移决策（7 KEEP + 6 REDESIGN + 0 DROP，含别名域拆二）；关键行为（正负证据三档阈值、临时换声状态机、固定音色优先级链）见 `research/third_party/legado-v907/V907_CAPABILITY_MATRIX.md`。

### 3. CosyVoice3-MNN 基线
```text
源仓库: E:\AndroidStudioProjects\CosyVoice3-MNN-formal（v1.1.0 / MNN 3.6.1 / minSdk 26）
主链冻结: LLM → Conditioner(子进程) → 2-step Flow → HiFT → 24kHz WAV
后端: LLM CPU（SM8850 仅第0层 q_proj NPU）；Flow CPU/OpenCL；HiFT CPU
性能: Magic8 Pro 热态 RTF 0.79–0.96；进程内存 ~947MB（VmRSS）
模型包: mnn-model-3.6.1 全量可用（int4 LLM + fp16 Flow2step + fp32 HiFT），enrollment 扩展 + 胡桃音色样例在库
```

## 已通过 Gate

- TASK-000（Repository Governance + Baseline Freeze）：2026-08-12 PASS。
- TASK-000.1（Baseline Hardening / Secret Quarantine / Git Bootstrap）：2026-08-12 PASS（权威统计全仓唯一、raw 凭据隔离、redacted 副本就绪、secret scanner 带 synthetic fixture、.gitignore + git init + baseline commit、12 域/13 决策口径统一、user-correction 结论降级为 NOT FOUND、verify_baselines.js 可重复运行）。
- **TASK-010（Immutable TXT Ingestion Layer）：2026-08-12 PASS（12 项 Gate 全过）。**
- **TASK-020（Chapter/TOC/Volume Structure Compiler）：2026-08-12 PASS（G1-G12 全过，63 测试全绿）。**
- **TASK-030（TXT Layout Profiling & Logical Paragraph Recovery）：2026-08-12 PASS（G1-G12 全过，G13 generalization=OPEN，全套件全绿）。**
- **TASK-040（Room Persistent Schema / Revision Graph / Dependency DAG）：2026-08-12 PASS（G1-G16 全过；G17 ROOM_RUNTIME_GATE=OPEN，JVM SQLite reference）。**
- **TASK-060（Revisioned Character Identity / Model B）：2026-08-12 PASS（G2-G19 全过，14 测试全绿；Schema v2 +10 表迁移；非破坏 merge；双查询 API）。**
- **TASK-070（Gold Set）：2026-08-12 PASS（G1-G12 全过；SemanticSegment / Rule-only Speaker Baseline / NarrationIR v1 / ContextBuilder / Dataset Exporter；MODEL ENTRY FREEZE 生效，ADR-033）。**
- **TASK-080（ReaderDirector Model Baseline）：2026-08-12 PASS（G1-G18 全过；三线对比同一 locked test：Rule Speaker 1.00 / 0.8B post 0.111 / 2B post 0.556；Base probe JSON valid 0%；判定=情况 B——2B 明显>0.8B，TASK-090 LoRA 0.8B-Base 主线不变，2B 作 Teacher；hf-mirror 下载 + torch 2.11.0+cu128/transformers 5.12.1/ms-swift 4.4.2 环境锁定；test_sha256=1bdb5d8f…；ADR-034 模型链修正）。**
- **TASK-090（ReaderDirector 0.8B-Base Multi-task LoRA）：2026-08-12 PASS（G1-G20 全过；口径修正：2B=CAPACITY_SIGNAL(Fisher p=0.131)、Base 0%=INVALID_TEMPLATE_PROBE(chat_template.jinja stub 修复后 VALID probe=协议100%决策退化)、UNKNOWN prov=UNLABELED_POOL；SFT v1 5,592（置换×4+id-echo 400+TRUE_UNKNOWN 300+Identity 92+Voice 状态机 200 均衡）；消融 r8 胜出（dev SPEAKER 0.867/perm 0.692/IDENTITY 1.0/TRUE_UNKNOWN 1.0）；locked test r8 = SPEAKER 0.333（3×于 0.8B post 0.111，2B 上界 0.556 之下）、IDENTITY 1.0、strict 91%；结论：0.8B 能学；已知限制：4+候选名字回显（TASK-090.1 dev 验证）。**
- **TASK-090.1（Candidate-Cardinality Robustness Patch）：2026-08-13 PASS（G1-G12 全过；post_cue 可见化 speaker_v2；生成器 gold 对齐 bug 修复（旧 test_v2/SFT v1.1 作废重生成重锁：test_v2=24b0711c、sft_v1.1=29630abb）；只训 r8/a16；DEV-CARD Acc@2..6 全 1.000、ΔK=0、perm@K=1.0（分布内，同模板诚实标注）；TEST_V2 80/80 全对；TEST_V1 诊断 0.667（↑自 0.556）、dev voice 0.714（↑自 0.429）；TASK080_TEST_V1 降级 EXPOSED_DIAGNOSTIC；2B = Teacher Candidate 定位修正）。**
- **TASK-095（Speaker Candidate Architecture v2）：2026-08-17 进行中（插入 TASK-100 之前；ADR-035/036/038/039；ExecPlan PLAN-20260816-095）。095A PASS（Candidate Recall 85.4%、polluted 0）；095B.2 PASS（AI Mention Discovery 定案，ADR-038）；095B.3 CONDITIONAL_FAIL/ACCEPTANCE_PENDING（正式状态，2026-08-18 口径修正）：0.8B MENTION LoRA checkpoint-783 训练完成（Exact Recall 88.2%==2B few-shot、Contain 91.9%、Precision 90.0%、Exact-span validity 100%；强域 transliterated/english/chinese 100%、弱域 nonhuman_agent 58%/compound_surname 77%/title_office 79%）；**audit_300（222 条人工审核）FAIL**：Semantic Recall 85.4%/Precision 87.6%/True NONE 81.8%（Gate 95/90/95）；归因：截断（max_new_tokens=200）+ 非实体当实体（24 条 FALSE，作品/物品/地点/病毒/律法/泛称）+ NONE 污染 6/33 → **ADR-039 Type Policy** + 定案 v1 prompt+400 tokens（Dev-QC 四轮：v2 规则文本/v3 示例化/v1+400/v1+动态）→ 当前环境全量重标 mention_train_v2（4250 条）**已完成（2026-08-18，4250/4250）**；A2 预检：SPEAKER 0.733↑/IDENTITY 1.0/TRUE_UNKNOWN 50/50 ✅、**VOICE_STATE 0.714→0.143 ❌** → v2 训练混合调整（MENTION 40% + VOICE_STATE ×6）→ **v2 训练完成（2026-08-18：checkpoint-780 epoch=1.0，mention_r8_v2/v3-20260818-212221；to_swift 重建 sft_v2_mention_v2.jsonl 12,473 条 sha256=38582ad3…）** → A2 复检：SPEAKER 0.733/IDENTITY 1.0/TRUE_UNKNOWN 50/50/strict 1.0 ✅、**VOICE_STATE 0.286（↑自 0.143，gold 7 条）**；**A3 regression vs v1（2026-08-18，checkpoint-780，Cross-domain Dev Challenge v1 = 原 challenge 150）**：Exact Recall 89.2%（↑88.2%）/Contain 93.5%（↑91.9%）/Precision 89.7%/Exact-span 100%，弱域 compound_surname 85%/title_office 86%/apostrophe 90%（均↑），nonhuman_agent 58% 持平 → **A3 定性 = REGRESSION PASS（vs v1），非 Acceptance PASS**；**capability gate = FAIL**（Semantic Recall 93.5 < 95、Precision 89.7 < 90、Validity 100% ✅）；**A4 Agent 初判完成（2026-08-18，221 条）**：Silver 数据质量 Semantic Recall 98.8%/Precision 85.8%/True NONE 90.9%（Gate 95/90/95 → FAIL）→ **A4 人工确认 PENDING**；MISS 4（呼语/称谓）、FALSE 54（ADR-039 非实体为主，host 过滤层未落地）、NONE 污染 3/33；**当前状态 = CONDITIONAL FAIL / ACCEPTANCE PENDING（收敛阶段，暂不进入 095C）**；**A5 完成（2026-08-18）**：扩 VOICE_STATE dev 至 75 条均衡（5 动作 × 15，dev_voice_state_v1.jsonl）→ **VOICE_STATE 无灾难... (line truncated to 2000 chars)

```text
TASK-040 PASS: Database Schema v1 frozen（27 表）/ Revision protocol frozen / Dependency invalidation contract frozen
实现栈：JVM SQLite（sqlite-jdbc）+ Kotlin interfaces；Android Room 未实测（本机无 SDK）
ADR-022~027 落地；Insert-or-replace 红线入 AGENTS
已知 STORAGE_PERF_DEBT：3.3M 字书 DB ≈ 322MB（~810k 行）——诚实记录，优化留 StorageManager
```

```text
TASK-030 Engineering Gate = PASS
Real-world generalization Gate = OPEN（20-30 真实 TXT Gold 未建立）
段落恢复冻结：Profile→Boundary→BlockGrouper→Paragraph+SourceMap+Links（ADR-018~021）
存储：LogicalParagraph Option B（spans+transforms，ADR-019）
3M+ 性能：166k 行 / 59,289 段 / 峰值堆 154MB（JVM Reference，非 Android Gate）
```

```text
TASK-020 functional Gate = PASS
Real-world generalization Gate = OPEN
Requires 20-30 real TXT books（当前 gold 为工程回归集，不宣称泛化准确率）
TASK-020 3M+ 性能（8.9MB → 172MB 峰值堆）= JVM Reference 通过，非 Android Memory Gate
```

## TASK-010 冻结事实（Immutable TXT ingestion baseline）

```text
实现栈：parser/ Kotlin/JVM 模块（Gradle 8.14.4 / Kotlin 2.3.10 / JUnit5）
编码：UTF-8/UTF-8 BOM/GBK/GB18030/UTF-16LE/UTF-16BE；纯 ASCII=UTF-8 1.0；GB18030 仅 4 字节序列时声称
Offset：byte + codepoint 双轨（ADR-013）；char→byte 精确表扫描时产出
存储：raw_text 不落库，offsets + lazy read（ADR-014，实测 2.7MB vs 12.3MB / 99ms vs 229ms）
导入：状态机 NEW→COPYING→HASHING→DECODING→INDEXING_LINES→IMPORTED；原子 rename；EXACT_DUPLICATE 按 sha256；
      revised edition 记录 parent_source_file_id；crash 后 recover 幂等；错误枚举 9 类
大文件：3,300,040 字符 / 116,472 行 / 364ms / 峰值堆 269MB
协议：docs/protocols/TXT_SOURCE_SCHEMA.md + SOURCE_OFFSET_CONVENTION.md
实验：docs/experiments/TASK010_ENCODING_REPORT.md + TASK010_LARGE_FILE_REPORT.md
fixtures：tests/fixtures/txt/（15 个自制文件；生成器 tools/gen_fixtures/FixtureGen.java 可复现）
```

## 优化支线状态

- **MOBILE-001 App Shell / Structure（2026-08-21）**：`parser-core` 复用 TASK-020 章节/源扫描源码和 26 条 Legacy RulePack；Android USB 真机对已导入私有 TXT 完成 v3 自动结构迁移，得到 620 个 CONFIRMED anchor，章节页显示完整原始章节头，`source.txt` SHA 前后完全一致。自动结构有 `structure_origin=AUTO_LEGACY_RULEPACK`，未来 `USER_EDITED` 不允许自动重编译。Host `:parser:test :parser-core:test :app-android:testDebugUnitTest :app-android:assembleDebug --offline` PASS；用户通过 SAF 再选同一 TXT，确认 ExactDuplicate、Book Package 数仍为 1。**M1 Device Gate PASS；M2 仅可开始 fixture cache-first playback，不得宣称 Director/TTS 已接入。**证据：`PLAN-20260821-052-mobile-001-structure-compiler.md`、`runs/mobile_001_structure_device_gate/report.md`。
- **MOBILE-002 M2.1 Cache Consumer（2026-08-21）**：Android 已实现 `audio/` WAV cache discovery、`book.db.audio_asset` 登记和已有 WAV 的串行 `MediaPlayer` 消费；Host scanner test/APK、USB 无缓存 UI 均 PASS。连续缓存播放仍 OPEN：`run-as` 不能从 `/sdcard` 读取 fixture，未写入音频且空目录已清理。不得把这一实现称为 CosyVoice/TTS 或 Scheduler Gate PASS。证据：`runs/mobile_002_cache_playback/report.md`。
- **MOBILE-002 Speech Runtime Contract（2026-08-24更新）**：ADR-045 冻结第一版按叙事身份分流：旁白/未知→Audio8，具名角色→CosyVoice3-MNN；目标 engine 未通过设备 Gate 时 fail-closed，不静默切换。Android Contract/Router unit test + APK PASS。Audio8 独立工作区的高质量 teacher codec 已通过 MNN-QNN HTP 数值/性能 Gate，但完整 text→wav 和 ReaderVoice APK 集成仍 OPEN；CosyVoice JNI runtime 也尚未合入共同 runtime。证据：`PLAN-20260821-053-mobile-002-speech-runtime-contract.md`、`E:/AndroidStudioProjects/audio8tts-mnn/docs/reports/AUDIO8_MNN_QNN_HTP_20260824.md`。

- **MOBILE-004 ReaderDirector QwenEngine 可视化调试台（2026-08-26，App 主线）**：M1 host Gate + **M2 真机 Gate 双 PASS**。QwenEngine 有状态 native service 七接口；ReaderDirectorDebugActivity 三层透明输出（Input 编辑框 → 最终 Prompt 可复制 → Raw Output → Parsed Result → Debug 指标）。**真机闭环**：423MB readerdirector-mnn-v2 部署到 App internal 目录（hash 核验），手动 GENERATE 得到完整结构化结果——`speaker=药老 / type=dialogue / text=你终于回来了。（去引号）/ emotion=neutral / delivery=normal / Parse=PASS`；`Backend=cpu / Generate 3018ms / load_us 734ms / prefill 1.24s / decode 985ms / gen_seq_len 69`。**Backend=cpu 是 M2 冻结预期**，HTP Correctness=NOT PASSED 未混入 App Gate。JSON Parser 容忍模型标点瑕疵（extractJsonObject 括号匹配）。HTP 支线（Branch A，lm_head 输入 cos=-0.0067）独立并行。证据：`PLAN-20260826-056`、`runs/mobile_004_director_engine/m2_gate_pass.md`。

- **MOBILE-004 ReaderDirector QwenEngine 稳定服务（2026-08-26，App 主线切回）**：方向定案——App 主线 + HTP 支线并行；App 不再等 HTP 0.99 收口。M1 host Gate PASS：QwenEngine 有状态 native service（`qwen_engine_jni.cpp` + `QwenEngine.kt`/`ReaderDirectorSchema.kt`/`ReaderDirectorEngine.kt`）七接口（load/reset/generate/cancel/release/getBackend/getMetrics，cancel 为协作式非杀线程）；ReaderDirectorSchema host 校验器（evidence_span 逐字校验、fail-closed、UNKNOWN 合法）单测 5/5 绿；assembleDebug 成功，两个 JNI so（qwen_engine_jni/readervoice_director_jni）16KB ELF 对齐（LOAD=0x4000，ADR-048）。**真机 M2 Gate 未开始**（App 未装机，423MB 模型在设备 shell 侧 `/data/local/tmp/MNN/model_rd/`，需装 APK + 部署模型到 App 私有目录）。HTP 支线状态：Branch A 成立（lm_head 输入 CPU vs HTP cos=-0.0067），问题在 L23/final-hidden 链，非 lm_head 算子。证据：`PLAN-20260826-056-mobile-004-readerdirector-engine.md`。

- **MOBILE-003 Native Artifact / Director common-runtime（2026-08-22）**：Magic8 Pro 的 16KB compatibility warning 已定位为预编译 Cosy/MNN 的 ELF `PT_LOAD=0x1000`，不是 APK zip alignment；以 NDK r27 16KB flags 从 MNN 3.6.1 重建的 shared runtime 与 ReaderVoice JNI 均已通过 ELF + APK Native Artifact Gate，debug APK 可安装。**冻结 423MB ReaderDirector int4 v2 在 ReaderVoice runtime 和重新安装的历史 Probe runtime 中都 `Llm::load()=FAIL`**（路径、六文件 SHA、伴随 JSON 已核对）。因此问题不能归为 ReaderVoice common-runtime 独有；历史 30 条 CPU PASS 实际使用不同的约 3.5GB FP16 base artifact，不能外推为 int4 PASS。下一 Gate 是 int4 export artifact 审计，GPU/OpenCL、QNN、RenderUnit 仍停止。Cosy 旧 bundle 已隔离保留，其无源码的 4KB conditioner 继续 BLOCKED。证据：`PLAN-20260822-054-mobile-003-readerdirector-android-runtime.md`、`runs/mobile_003_director_runtime/report.md`、`p0a_runtime_provenance.md`、ADR-047/048。

- ReaderDirector Qwen3.5 MNN/QNN（SM8850，2026-08-21）：**MNN CPU Runtime PASS；QNN HTP BLOCKED**。QAIRT 2.48 的 `87/v81` SDK/WSL host 可用，但本机 MNN 3.6.1 QNN offline converter 无法编码冻结 hybrid 图（`FusedRoPE` type 306、Binary/Mul、`in_proj_z`/`q_gate_proj`/`lm_head` errors）；无完整 context，未构建/安装 QNN APK。此结论不否定 Qwen3.5 MNN runtime 支持，也未用官方 base 模型对照，不能归因于 LoRA merge。QNN 是后续 Director Accelerator 支线，不阻塞 Android 阅读闭环；取得更高 MNN revision 或精确匹配的官方 QNN artifact 后从 M1-M4 重做，详见 `PLAN-20260821-051-mobile-000b-qnn-sm8850-spike.md`。
- **M0.5 ONNX unfuse + QNN 直转（2026-08-23 结论）**：绕开 MNN converter 限制的直转路线已完整打通并在真机链式解码（3 段，SM8850），但 **a16w8 静态量化无法到达等价 Gate**（logits cos 0.37-0.46，top1 不匹配）；per-head 状态缩放恒等验证通过却无精度增益（h7 0.884→0.880，logits 0.462→0.371）。结论：**M0.5 路线终止，转 Road B（MNN HexagonLinearAttention 后端，官方 3.6.1 Hexagon fp16 路线，pp512=2667 tok/s @SM8750）**；M1 skeleton 已提交 afb1c24，decode DSP 命令实现中。证据：`runs/mnn_llm/qwen35_qnn_unfuse/progress.md`、`seg-chain-gate-20260823-172944/`、`PLAN-20260822-055-mobile-000b2-hexagon-linearattention.md`。
- **Road B M1/M2-0（2026-08-24）**：fork `MNN-3.6.1` HEAD `5d0faf1` 完成 HexagonLinearAttention decode(L=1) 命令序列（全复用 DSP primitive，零新增 kernel）；代码复查修正 **create 期 shape 判定 fallback**（Pipeline 只在 create 返回 nullptr 时回落 CPU；onBuildCmd NOT_SUPPORT 是运行失败），hexagon factory 已按 Softmax 同款 4 参签名传入 inputs/outputs。架构调整：**prefill(L>1) 留 CPU，decode(L=1) 上 HTP**，prefill→decode 边界一次状态 handoff（CPU mStateCache ↔ HTP mConvState/mRecurrentState，不逐 token 同步）。M2 门序见 PLAN-055 修订节；冻结基线暂不 rebase master。
- **MOBILE-005 / M0 PASS：rd-mnn-v2 能稳定执行 selection-style 协议（2026-09-16）**：`docs/agent/plans` 的 PLAN-058（Segment-ID 选择协议版）已批准，本轮执行 M0 探针。素材：真实书《娱乐：从1990年开始》4000 段 → 桌面对拍管线（PhysicalLineScanner → ParagraphRecoveryPipeline → SemanticSegmenter → RuleSpeakerBaseline → SpeakerCandidateCompiler/ContextBuilder）产出 **25 个目标 speech segment**，候选 = 生产候选 + 每样本确定性 permutation（与 DECISIONS 冻结设计一致），候选召回 25/25、gold 强度 CONFIRMED 21/PROVISIONAL 4、候选数 2~4。冻结 greedy + 模型 MD5 `f38be9f4…` + P15_REPEAT=3。**结果：P1 JSON 25/25、P2 segment_id 回填 25/25、P3 speaker ∈ 候选∪UNKNOWN 25/25、P4 evidence ⊆ 提供集合 25/25、P5 gold 一致率 19/25 (76%)、P6 UNKNOWN 率 0%、三次运行 100% 逐字一致 ⇒ PASS**。⇒ **M5（导出候选协议模型）不触发**；"让模型输出 text_span"的设计取消，正文永远取 canonical sourceSpan（文本漂移在架构上不可能发生）。三条必须带上的观察：① 协议卫生 cosmetic=150（模型把 `C2`/`neutral` 写成裸 token，Validator 必须容忍并计数）；② UNKNOWN 率 0%（即使选错也照选 ⇒ M3 需显式评估 UNKNOWN 触发条件，否则是"假确定"）；③ 6 个错例全是候选内选错（非越界），属语义/候选集问题。证据：`runs/mobile_005_director_real/m0_protocol_probe/{report.md,fixture.jsonl,out_*.txt,judge_result.json}`、`data-room/src/test/.../m0/M0SelectionFixtureTest.kt`、`tools/mobile005/{judge_selection.js,run_m0.ps1}`。
- **MOBILE-005 / M1 PASS：设备端段落化与桌面位级等价（2026-09-16）**：`parser-core` 是对 `parser/src/main/kotlin` 的 sourceSet 视图，段落包一直被 `kotlin.exclude(".../paragraph/**")` 排除 ⇒ App（只依赖 parser-core）拿不到 `LogicalParagraph`。**M1 = 删掉那行 exclude**（改前先核验依赖闭合：paragraph 只 import `source.PhysicalLine` + `java.lang.Character.UnicodeScript`，后者 Android API24+ 可用；`ImportPipeline` 保持排除）。新增 Gate 工具：`ParagraphParityActivity`（设备端 dump，调用路径与 `BookRepository.compileStructure` 一致）+ `M1ParagraphParityTest`（桌面同格式 dump）+ `tools/mobile005/paragraph_parity.js`（判分）。**结果：同一输入（App 内真实书 source.txt，md5 `20576d75…`，bookId 两端一致）desktop=34566 条 / device=34566 条，`pid/rid/idx/ch/block/policy/conf/len/sha256(normalizedText)` 全部相等 ⇒ G1 PASS（位级等价）**。设备端整本 3 MB 书段落化 24,559 ms（≈0.7 ms/段，peak 内存 ~86 MB，无 OOM）；M3 按章切 lines 即可，无需整本跑。⇒ M2 的移植面只剩"抽 CharacterStore 接口 + 加 segmentId/evidenceItems 字段"。证据：`runs/mobile_005_director_real/m1_paragraph_parity/{report.md,input.txt,desktop.jsonl,device.jsonl}`、`app-android/.../ParagraphParityActivity.kt`、`parser-core/src/test/.../M1ParagraphParityTest.kt`。
- **分章导演 + 剧本不丢 + 看见真实文本（2026-09-18）**：用户反馈三点（"不是分章导演吗/剧本也该分章"、"准确度一般"、"看不见真实文本"）。**① 剧本丢失 bug（根因）**：服务每轮 `jsonl.writeText("")` 清空同一文件，且行数只在内存 ⇒ 退出后变 0。**修复**：每轮写独立归档 `scripts/<bookId>_<scope>_<ts>.jsonl`（历史不再被清空）+ 镜像 `script_lines.jsonl`（保持既有工具/校验脚本可用）+ 进程重启时从镜像恢复行数。**② 按章导演**：章节行【导演本章】⇒ 服务按 `LogicalParagraph.chapterId` 过滤（无需改索引格式；不存在的序号回退整本）；服务与 UI 共用同一套**用户规则选择**（书包内 `chapter_rules.json`，随书走/切书隔离/原始终不可变 C1）。**③ 章节规则（重新分章）**：新增 `ChapterRulesActivity` 列出规则包全部规则（族/目标/正则）可逐条勾选 ⇒ 重跑 `ChapterStructureCompiler` ⇒ 原子替换章节目录 ⇒ 显示"识别到 N 章"；`BookRepository` 增 `chapterRules()/recompileChapters()`，`compileStructure` 会应用用户选择。**④ 看真实文本**：新增 `ReaderActivity` 原文阅读器——按章列出 + 点进去看**真实正文**（用 `Chapter.contentStartLine..contentEndLine` **行号区间**取文，不受编码/字节偏移影响）+ 上下章翻页；**没有原文就无法判断准确度**，这是提准确度的前提。**⑤ 角色发现缓存**：`characters_<bookId>.json`（按源长度+mtime 判命中）⇒ 全书发现不再每轮重扫。**⑥ 进度与回跳**：运行台加进度条（done/total）+「查看剧本」+ 完成 2.5s 自动回跳；`ScriptViewActivity` 改读**最新归档**并显示归档份数/章节归属。**验证**：编译+装机成功；章节页已出现【看原文/导演本章/章节规则（重新分章）】；原文页已能显示章节标题（实测「第2章 能力测试」）。**未取得干净证据**：原文正文逐字确认（UI 驱动正则不稳 + 手机被并发使用）；剧本视图的整本运行"章内分组"待 run 时落 `outline_<bookId>.json` 侧车。**准确度**：属模型层/候选层（CH-1 + P2 hard negative + 候选污染指标），本轮未用 UI 假装修准确度。证据：`runs/mobile_005_director_real/chapter_script_ui/report.md`、`app-android/.../{ReaderActivity,ChapterRulesActivity,ChapterRuleSelection,ScriptViewActivity,ChapterDirectorService}.kt`。
- **产品入口补齐：怎么让它开始导演（2026-09-18）**：此前导演**只能靠 adb 命令**启动（`am start ... --ez autorun true`），UI 无入口。**改动**：① `ChapterListActivity` 新增 **【开始导演（前 160 段）】** 主按钮：说明文案（本机 0.8B 逐段判定"谁在说、怎么说"，旁白段不消耗模型调用）+ `startForegroundService(ChapterDirectorService.startIntent(...))` 后自动打开运行台；默认批 = 前 160 段 / 模型调用上限 60。② `ChapterDirectorActivity` 修一个真问题：进入运行台时**不再无条件覆盖正在运行的服务日志**（原来 `onCreate` 写"待运行…"会把实时日志冲掉）。**为何 v1 用"前 160 段"而非选章**：`ChapterIndexEntry` 只有 `ordinal/title/anchorByte/state`，**缺「章→段落下标」映射**，而导演范围按段落下标切（记为待办）。**USB 真机实证**：launcher 冷启动进书架 ✓ → 点书进章节页 ✓ → 点【开始导演】⇒ `com.readervoice.app` 前台服务 `isForeground=true` ✓（该服务只有 adb-autorun 与按钮两个入口，本次无 extras ⇒ 按钮触发）→ 自动打开运行台（`mCurrentFocus=ChapterDirectorActivity`）✓ → 剧本实时产出 `script_lines.jsonl` **34→46 行/45s** ✓。**用户操作路径**：打开 App → 书架点书 → 章节页点【开始导演】→ 运行台看进度（可切走 App/锁屏，前台服务不中断）。**待办**：按章导演（补段落下标）、进度条与完成回跳、角色发现缓存、剧本可视化（`script_lines.jsonl` 已含全部字段）、TTS 接入（CH-2）。证据：`runs/mobile_005_director_real/start_directing/report.md`、`app-android/.../ChapterListActivity.kt`。
- **修复"没有导入书籍"：产品入口还给书架（2026-09-18）**：用户报告"没有导入书籍"。**根因不是缺功能，而是 launcher 被临时改挂在调试页上**（Manifest 里 `ReaderDirectorDebugActivity` 与 `VoiceDesignActivity` 各带一个 MAIN/LAUNCHER）⇒ 打开 App 直接进调试台，书架与「导入 TXT」入口根本不可见。**修复**：① `MainActivity` 迁 Jetpack Compose（Material3 + safeDrawing insets）：标题/副标题 + 「导入 TXT」按钮 + 状态反馈 + 书架 LazyColumn（卡片：书名 / N 项 · 目录状态），点卡片进 `ChapterListActivity`；② Manifest **launcher 归还 `MainActivity`**，两个调试页去掉 MAIN/LAUNCHER（保留 `exported=true` 供 adb）。**导入链路口径不变**：SAF `ACTION_OPEN_DOCUMENT`(text/plain，兼容 octet-stream/*) → `BookRepository.importDocument(uri)` → 原文复制进私有 Book Package（不可变）+ 目录编译；重复原文走 `ExactDuplicate`；启动时后台补编译（`upgradeGeneratedPackages`）。**USB 真机实证**：`cmd package resolve-activity -c LAUNCHER` → `.MainActivity` ✓；冷启动进书架 ✓；点「导入 TXT」拉起系统文件选择器（`documentsui/PickActivity`）✓；书架列出「我的脑力无限进化 作者：那一只羊 ／ 249 项 · 目录已生成」✓；点书进章节列表「已生成 475 个可靠章节锚点」+ 第1/2/3章真实标题 ✓。**待办**：其余 7 屏仍纯 View（建议续迁 Compose）；导入后仅文字反馈（可加进度条）；章节列表"播放"仍无真实 TTS（属 CH-2）；建议 CH-1 顺带做「全书角色发现缓存」。证据：`runs/mobile_005_director_real/book_import_fix/report.md`、`app-android/.../MainActivity.kt`、`AndroidManifest.xml`。
- ★ **MOBILE-005 CLOSED（2026-09-18）**：`2026-09-17-mobile-005-real-chapter-director` 经 **Close Gate ALL PASS** 后正式关闭（Normify `phase=verified`，0 error，open change 5→4，revision.after=f9ff5b26）。**Gate 结果**（USB 设备 A3TE025B03003242，book-4455b46ef2d2 段 60..200，hexagon）：**3 次完整整章各 138 行、coverage=98.6%、acc=137/down=1/rej=2、shortcut=111、calls=29、noOutput=0、elapsed=308/318/329s**，且**统计逐项完全一致**（决策链确定性成立）；**第 4 轮主动 epoch cancel**：点击「取消（epoch++）」⇒ `canceled=true`、**已产出 50 行全部保留**、`watchdog stuck=0`（证明是协作式取消而非超时/卡死）。**9 条 Close Gate 逐条核对**：anchor 100%（138/138×3 + 50/50，文本含 JSON=0、顺序单调）✓ / permanent hang=0（watchdog timeouts=0 canceled=0 stuck=0 ×4）✓ / crash·OOM=0 ✓ / timeout 机制就位且 fail-closed ✓ / epoch cancel 真机 PASS ✓ / 每段增量落盘 ✓ / 悬空 C# 仍 REJECT（SPEAKER_TYPE_CONFLICT=2，0 处偷改）✓ / 同章 3 次完整 ✓ / 指标记录（运行级 P50=318s、P95=329s；★ 单次调用延迟**不可得**：该机型 QwenEngine logcat 缓冲区加密，已如实记录）✓。★ **C18 真机终证明**：4 轮共 **464 行**持久 ScriptLine 中 `schemaVersion!=2`=0、旧 `speaker` 字段=0、**局部候选 ID 泄漏=0**、非法 speakerRef=0，分布 {NARRATOR:378, CHARACTER:88}。**跑通前提**：整章运行已迁**前台服务宿主**（每轮启动后主动 HOME 切后台仍在推进）。**遗留（不阻塞）**：① 单次调用 P50/P95 需改为落盘指标；② 全书角色发现未缓存（内存紧张时是主要成本，建议并入 CH-1/CH-3）；③ UNKNOWN/VERIFY 本章为 0（协议合法未触发）；④ 2 个 `SPEAKER_TYPE_CONFLICT` hard-negative 样本待沉淀（P2）；⑤ 其余 8 个界面待迁 Compose；⑥ 债务 `render-unit→freeform-legacy`（owner CH-1/CH-3）。证据：`runs/mobile_005_director_real/close_gate/{report.md,run_{1,2,3,run_cancel}/**}`。**下一里程碑：CH-1 character-identity-promotion（PLAN-20260918-059）**。
- **整章运行迁入前台服务宿主 + Freezer 免疫实证（2026-09-18）**：此前两次采集事故（JSONL 停在 43 行/28 分钟、停在 27 行）根因确认——**整章运行挂在 Activity 生命周期上，Activity 不可见 ⇒ 进程 cached ⇒ 被 Android Freezer 冻结**（不是 native 卡死、也不是看门狗失效）。① **新增 `ChapterDirectorService`**（`foregroundServiceType=dataSync`）：前台服务承载整章运行 + 常驻通知（进度 + 「取消（epoch++）」动作）+ 进程内 `ChapterRunBus`（StateFlow: log/summary/running）供 UI 镜像；`onStartCommand` 处理 `ACTION_START/ACTION_CANCEL`（取消仍是 epoch 协作式，绝不杀线程）。② `ChapterDirectorActivity` **退化为 UI 壳**：Compose 界面 + `startForegroundService` 触发 + `collectAsState` 订阅 + 取消按钮发服务意图；`autorun` 入口与用法完全不变。③ Manifest 注册 service。**★ 证据口径仍冻结**：同 extras、同 runner/watchdog(30s/15s)/maxTokens=96、同产物 `files/chapter_director/{script_lines.jsonl,stats.txt}` 与 JSON 字段名、同 `ChapterDirector` 日志标签。**★ Freezer 免疫实证**：安装后面板 `am start -W`(334ms) → 等 20s(7 行) → **`input keyevent KEYCODE_HOME` 切后台（=两次事故的同一条件）** → `dumpsys` 显示 `isForeground=true types=0x1` → 再等 75s **JSONL 7→56 行仍在推进** ✓✓，且首行为 `{"schemaVersion":2,…,"speakerRef":"NARRATOR","route":"AUDIO8_NARRATOR"}`（C18 v2 落盘）。**⏳ 阻塞**：Close Gate 3 次完整+1 次取消被**设备内存压力**阻塞（`14881M used / ★257M free`、swap 8.6GB；`chapter-director` 线程 5 分钟仅 0:17 CPU、sleeping、`chapter_director/` 目录尚未创建）⇒ **整机换页爬行，属环境问题非代码问题**；处置：释放内存（关后台 App/重启）或缩小批次。已备好 FGS 版 Gate 脚本与证据校验脚本（`tools/mobile005/{run_mobile005_close_gate.ps1,verify_close_gate.js}`）。证据：`runs/mobile_005_director_real/fgs_host/report.md`、`app-android/.../ChapterDirectorService.kt`。
- **UI 重做（Jetpack Compose）+ 状态栏/安全区（2026-09-18）**：按用户选择走 **Compose 路线**。① **诊断**：9 个界面里 6 个完全没有 insets 处理（ChapterDirector/ReaderDirectorDebug/CosyVoiceM2Probe/DirectorM1Gate 硬编码 setPadding；ParagraphParity/SemanticParity/VoiceDesign 无处理），另 2 个用了**已废弃**的 `systemWindowInset*`；`targetSdk=35` ⇒ Android 15 强制 edge-to-edge ⇒ 内容被状态栏/手势条压住。② **工具链**：根 `build.gradle.kts` 加 `org.jetbrains.kotlin.plugin.compose 2.3.10`（与 Kotlin 对齐）；`app-android` 加 compose 插件 + `buildFeatures.compose=true` + `compose ui/graphics/foundation 1.9.3 + material3 1.4.0 + activity-compose 1.11.0`（版本与本地缓存对齐）；**`compileSdk 35→36`**（AAR 元数据要求；**targetSdk 保持 35，运行行为不变**）；`gradle.properties` **`android.useAndroidX=false→true`**。离线核查：缓存已有 compose-ui/material3/activity-compose ✓，但缺 `lifecycle-runtime`/`savedstate`/**编译器插件 marker** ⇒ **首次必须联网一次**（已实渲网络可用，成功解析）。③ **设计系统** `ui/ReaderVoiceTheme.kt`：InkBlue/Accent 配色 + 深色模式 + 字阶 + **MonoStyle**（日志/ID 逐字核对用）+ 4dp 间距栅格。④ **运行台迁移**：`ChapterDirectorActivity` 由 `android.app.Activity` → `ComponentActivity`，纯 View → **Material3 Scaffold + TopAppBar + Card**，状态栏用 **`enableEdgeToEdge()` + `contentWindowInsets = WindowInsets.safeDrawing`**；★ **runner 逻辑与产物口径逐字冻结**（同 extras、同 watchdog、同 JSONL/stats 路径与字段名、同 `ChapterDirector` 日志标签、取消按钮仍为「取消（epoch++）」且仍是 epoch 协作式取消）。⑤ `:app-android:assembleDebug` 通过（APK 16.5MB）。**未完成**：其余 8 屏迁移 + 删除废弃 `applySafeDrawingPadding` + 装机验证（设备被 Gate 占用）。★ **顺带暴露的真问题**：Close Gate 两次失败均因**用户切走 App ⇒ Activity 进后台 ⇒ cached-app freezer 冻结进程**；**整章运行必须由前台服务承载**（架构模型中 `rvm.orchestration.foreground-service` 已如此标注）⇒ 建议优先做前台服务宿主，这是 3 次长跑的前提。证据：`runs/mobile_005_director_real/ui_compose/report.md`、`app-android/.../ui/ReaderVoiceTheme.kt`、`app-android/.../ChapterDirectorActivity.kt`。
- **MOBILE-005 / C18 落地：局部候选 ID 不再进入持久剧本（2026-09-18）**：按 2026-09-18 架构评审改序，**C18 被列为 MOBILE-005 的最后一块结构安全门，且必须先于 3 次长跑落地**（理由：本里程碑的最终产物就是持久 `ScriptLine[]`；若其中仍是 request-scoped 地址 `C0`，生产契约就没真正成立）。**实现**：① 新增 `SpeakerRef`（`@JvmInline value class CharacterId(稳定 opaque)` + `sealed interface SpeakerRef { Narrator | Unknown | Character(id) }`，线上表示 `NARRATOR`/`UNKNOWN`/`CHARACTER:<id>`）；② 新增 `CandidateMap`（C0..Cn→CharacterId，随请求死亡）+ `LocalCandidateResolver`（**纯查表、不做身份推断**）；③ `ScriptLine.speakerLocalId/speakerIdentityId` → **`speaker: SpeakerRef`**（附只读兼容访问器）；④ `ScriptLineCodec` 升 **schemaVersion=2** 写 `speakerRef`，**v2 不再输出旧 `speaker` 字段**；v1 旧产物走 `LegacyScriptLine`（`speakerResolvable=false`，**不猜历史 C0**）；⑤ runner 改为"先建行再计结局"，候选无法解析为持久身份时计 **REJECT + SPEAKER_ID_MISSING**（fail-closed，不静默丢行）；⑥ 同名冲突消歧：NarrationIR 的旧记录改名 `NarrationSpeakerRef`（`SpeakerRef` 一名一义）。**Gate**：★ 固定 24 段（34 片段）快速 Gate —— `lines=34 acc=34 rej=0`、**`localIdLeaks=0`**、anchor=100%、路由 AUDIO8=14 / COSYVOICE=20；★ 新增 `C18SpeakerRefGateTest` 7 用例（含 NARRATOR→Narrator、C#→稳定 id、悬空 C# 仍 REJECT、候选无法解析⇒fail-closed、codec 拒绝局部 ID、v1 只读）；★ **data-room 全量 25 suites / 122 tests / 0 failures**；`semantic-core`+`parser-core`+`app-android` 编译通过。**★ 最强论据（测试自证）**：stub 每段都答 `C0`，但 `C0` 在不同段映射到 `uid-0` / `uid-1`（`distinctCharacterIds=[uid-1, uid-0]`）—— 持久化 C# 必然指向别人。**剩余阻塞**：真机 3 次完整运行 + 主动 epoch cancel（设备当前不可达，无线端点失效/休眠），未完成前 MOBILE-005 不关闭。证据：`data-room/.../semantic/{SpeakerRef,LocalCandidateResolver,ScriptLineBuilder,ChapterDirectorRunner}.kt`、`data-room/src/test/.../m3/C18SpeakerRefGateTest.kt`、`docs/agent/plans/PLAN-20260918-059-ch-1-character-identity-promotion.md`（D7–D10 决策已冻结）。
- **MOBILE-005 / M3 真机验收 PASS：App 内整章闭环可用（2026-09-17）**：`ChapterDirectorActivity`（真机、真 0.8B、真章节）**140/140 段完整跑完**：`shortcut=111 model_calls=29 no_output=0`、`ACCEPTED_ALL=137 DOWNGRADE=1 REJECT=2`、**lines=138 coverage=98.57% failClosed=1.43%**、`watchdog: calls=29 timeouts=0 canceled=0 stuck=0`、elapsed 227.7 s（设备 loadavg ~17）。**用户验收判据逐条核对**：无 hang ✅ / 无 crash·OOM ✅ / coverage ≥96% ✅（98.57%）/ **anchor 100%**（138/138 行 `text.length == span`、0 行含 JSON 字段名、**0 处乱序**）/ 路由一致（`NARRATION/NARRATOR→AUDIO8`×111、`DIALOGUE/C#→COSYVOICE`×27）/ 全部 `status=PENDING` ✅。剩余 2 REJECT（1.4%，`SPEAKER_TYPE_CONFLICT`）与 1 DOWNGRADE（`DELIVERY_TONE_INVALID`）均为"按协议拒绝/降级"，**没有偷偷产出错误剧本**。★ **上一轮"卡死"根因澄清：不是模型、也不是看门狗失效，而是 Android cached-app freezer** —— Activity 被别的应用挤到后台 ⇒ 进程冻结 ⇒ 线程停推；同一次运行改【前台 + `svc power stayon true`】后 227 s 正常跑完。⇒ **产品化行动项：整章运行必须跑在前台服务**（App 已有 `VoiceDesignService(foregroundServiceType=dataSync)`，接管即可）；教训：**"后台长跑冻结"会伪装成"native 卡死"**，排查先看进程是否 visible/frozen。本轮实现：fork 加头文件内联 `requestCancel()/status()/resetStatus()`（两棵树同步；**有意不替换 libllm.so** 以保已验证 HTP 基线）+ JNI `nativeGenerateDeadline/nativeCancelGeneration` + `QwenEngine.GenOutcome` + `EngineWatchdog`（RUNNING→CANCEL_REQUESTED→CANCELLED/TIMEOUT→NO_OUTPUT；grace 超时⇒ENGINE_STUCK 暂停后续调用；native 调用跑独立线程）。**关键发现：MNN fork 的 AR 循环本就检查 `USER_CANCEL`/`timeout_ms`（generate.cpp:44）⇒ 无需改解码逻辑**。证据：`runs/mobile_005_director_real/m3_app_acceptance/report.md`、`app_chapter_run/{stats.json,script_lines.jsonl(138 行)}`、`m3_runtime_cancel/report.md`。
- **MOBILE-005 / M3：native 协作式取消 + 超时看门狗（2026-09-17，代码完成/真机验收待解锁）**：用户定案"当前唯一 blocker 是 native 调用的可终止性，不是模型能力"，且明确 **Kotlin `withTimeout()` 不够**（进了 JNI 后协程取消只取消外层等待）。① **关键发现**：MNN fork 的 AR decode 循环**本来就检查** `mContext->status == USER_CANCEL` 与 `param.timeout_ms`（`speculative_decoding/generate.cpp:44`）⇒ 不需改 MNN 解码逻辑；但 `getContext()` 返回 const 且 `mContext` 是 protected ⇒ 给 fork 加 3 个**头文件内联**公开方法 `requestCancel()/status()/resetStatus()`（两棵树同步：App CMake 用的 `MNN-3.6.1` 与构建 .so 的 hexdebug 树）。**有意不替换 libllm.so**：方法非虚且内联、编译进调用方 TU、.so 自身不调用 ⇒ ABI 不变 ⇒ 保留已验证的 HTP 基线（一次只动一个变量）。② JNI 新增 `nativeGenerateDeadline(prompt,maxTokens,timeoutMs) → "OK:<text>"|"TIMEOUT"|"CANCELED"`（每轮 resetStatus；prefill 后与每 token 间检查 wall-clock deadline；超时 `requestCancel()` 让 native 在下一 token 边界退出）+ `nativeCancelGeneration()`；`nativeCancel()` 也改为真正置 native 状态。③ Kotlin `QwenEngine` 增 `GenOutcome{Ok|Timeout|Canceled|NotLoaded}` 与 `generateDeadline/cancelGeneration`。④ 新增 **`EngineWatchdog`**（用户冻结状态机）：`RUNNING→CANCEL_REQUESTED→CANCELLED/TIMEOUT→NO_OUTPUT(继续下一段)`；**超 grace 判 ENGINE_STUCK 并暂停后续模型调用**（不再堆请求）；native 调用跑**独立线程** ⇒ 即使 JNI 内卡死调用线程也按时返回；epoch 过期结果丢弃。⑤ `ChapterDirectorActivity` decider 改走看门狗，stats 增加 watchdog 摘要。**验收状态：代码全部编译通过 + 设备安装成功，但真机验收被【设备锁屏】阻塞**（keyguard 需手动 PIN/图案解锁，`wm dismiss-keyguard`/上滑/WAKEUP 均无效；我无法代做）⇒ "113/113 完整结束、无 hang、无 crash/OOM、coverage ≥96%、anchor 100%"待解锁后立即执行。证据：`runs/mobile_005_director_real/m3_runtime_cancel/report.md`、`app-android/.../{EngineWatchdog,QwenEngine,ChapterDirectorActivity}.kt`、`app-android/src/main/cpp/qwen_engine_jni.cpp`、fork 的 `llm.hpp` 补丁。
- **MOBILE-005 / M3：整章满量运行 = 首个整章级 Gate 结果（2026-09-17）**：把本章全部 **35/35** 对白 prompt 跑满（shell `llm_demo`，HTP 0:23/greedy/96 tokens）。**结果：113 个可朗读片段 → 109 行 ScriptLine，coverage 96.5%、failClosed 3.5%、shortcut 69.0%、cosmeticFixes/call=3.00；outcome ACCEPTED_ALL=104 / VERIFY=5 / REJECT=4**。★ **剩余 3.5% 是【真协议失败】而非缺数据**：`SPEAKER_ID_MISSING×2`（模型给了候选外的 C#，**按契约 REJECT 且没有被偷偷改成 UNKNOWN**）+ `SPEAKER_TYPE_CONFLICT×2`。⇒ **G3a 锚定安全 100%（结构保证，109 行全部通过 canonical 断言、status=PENDING ≠ COMMITTED）、G3b 覆盖率 96.5% / fail-closed 3.5%（分开报告）**。另新增 **App 内闭环** `ChapterDirectorActivity`（设备侧自举角色发现 names=67 + `BootstrapCharacterStore` + runner，decider=`ReaderDirectorEngine.analyzeRawPrompt`，backend 走 `QwenBackendPolicy`，epoch 取消 + 逐行 JSONL + stats.txt）：跑起来并产出 56 行（旁白 NARRATOR + 模型裁决对白，文本均 canonical），**随后卡死在一次 JNI 模型调用**（18 分钟无进展、进程仍在、无异常）。**App 侧两个真问题**：① **已修** maxTokens=48 会把答案（50-60 token）截断 ⇒ 29/29 `JSON_PARSE_FAILED`，改 96（教训：协议类调用的 max_new_tokens 必须按答案长度给足，否则 Validator 只看到 JSON_PARSE_FAILED 看不出"截断"）；② **待修** JNI 调用缺超时/看门狗，卡死时无任何可观测信号 ⇒ 需加超时（超时计 NO_OUTPUT fail-closed）并让 epoch 取消能打断 native 生成。分层结论：**协议层成立（96.5% 覆盖、0 伪造）；运行时健壮性是新暴露的问题**。证据：`runs/mobile_005_director_real/m3_step56_full/report.md`、`chapter_run/{script_lines.jsonl(109 行),chapter_report.txt,plan.json,prompts/**,out_00..34.txt}`、`app-android/.../ChapterDirectorActivity.kt`、`tools/mobile005/run_chapter_batch.ps1`。
- **MOBILE-005 / M3 步骤 5+6 PASS：ChapterDirectorRunner 与真实章节端到端（2026-09-17）**：① **`ChapterDirectorRunner`**（逐 `SemanticSegment`）：`LogicalParagraph → SemanticSegment[] → DirectorContext →（shortcut | 模型）→ Normalizer → Validator → ScriptLineBuilder → 逐行回调落盘`；用户冻结的四条约束全部落地并各有测试 —— **协作式取消**（每段之间检查令牌，取消即停、**已产出行保留**、取消后不再调用模型）、**增量落盘**（每行立即回调 + `ScriptLineCodec` JSONL）、**旁白不问模型**（shortcut 合成决策但**仍走完整 Validator**）、**逐段 fail-closed**（一段 REJECT 不污染其它段，覆盖率与 fail-closed 率分开统计）。② **`BootstrapCharacterStore`**：设备无角色库时的自举只读 store（身份=名字、无聚类、声音状态默认；不假装有数据），证明"仅有角色名集合"即可跑通整章。③ **真实章节端到端（录制→设备→回放）**：`娱乐：从1990年开始` 第 60..160 段连续 100 段 = **113 个可朗读片段**，经 shell `llm_demo`（HTP 0:23/greedy/3 reps）执行 ⇒ `shortcut=78 model=35 noOutput=23`、`ACCEPTED_ALL=85 VERIFY=5 REJECT=23`（REJECT 全是"未执行的 prompt"）、**linesEmitted=90 coverage=79.6% shortcut=69.0%**。**三个结论**：(a) **shortcut 省掉 69% 模型调用**（113 段里 78 段是旁白）；(b) **UNKNOWN/VERIFY 路径首次真实出现**（`VERIFY=5`，路由 `UNKNOWN_DIALOGUE_FALLBACK`、`speakerIdentityId=null`），补上了此前两批次 UNKNOWN=0% 的缺口；(c) 90 行 ScriptLine **全部通过 canonical 锚定断言**，且全部 `status=PENDING`（ACCEPTED_ALL ≠ COMMITTED）。④ 自查修正两处指标口径：`cosmeticFixRate`（会 >100%）→ **`cosmeticFixesPerModelCall`**；并明确"协议指标分母=模型调用数、覆盖率分母=可朗读片段数"，避免 shortcut 稀释。⑤ 待办：本次只跑 12/35 条对白 prompt（按要求分批），补跑即可把覆盖率推到 ~100%；App 内直连 QwenEngine 的 `ChapterDirectorActivity` 未写（decider 即 `QwenEngine.analyze`）。**测试：M3 全套 46 tests / 0 failures**。证据：`runs/mobile_005_director_real/m3_step56_runner/report.md`、`chapter_run/{plan.json,prompts/**,out_*.txt,script_lines.jsonl,chapter_report.txt}`、`data-room/.../semantic/{ChapterDirectorRunner,ScriptLineBuilder}.kt`、`snapshot/BootstrapCharacterStore.kt`。
- **MOBILE-005 / M3 步骤 3+4 PASS：协议闭环冻结 + 第一批评估（2026-09-17）**：按用户定案"先冻结 DecisionV2 + outcome 语义，再写代码"。① **`DirectorDecisionV2` 冻结**（segmentId/speaker/type/emotion/emotionIntensity/delivery/voiceEvent/evidence，**绝不携带生产文本**；枚举含 `UNRECOGNIZED` 表示"映射失败"而非猜测）；`speaker/type` 关系冻结（NARRATOR→NARRATION→AUDIO8；C#→DIALOGUE→COSYVOICE；UNKNOWN→DIALOGUE→fallback；UNKNOWN+NARRATION 不存在）；**四态结局** ACCEPTED_ALL/DOWNGRADE/VERIFY/REJECT（UNKNOWN ≠ REJECT ≠ DOWNGRADE）；**失败码 15 个冻结**并分硬/软（悬空 `C7` ⇒ SPEAKER_ID_MISSING **绝不偷偷改 UNKNOWN**）。② **四层链路实现并测试**：`ProtocolNormalizer`（只改表示形式：裸 token/枚举大小写/围栏/`E0: RULE: xxx`→E0/扁平 delivery 键，逐项记账 cosmeticFixKinds；**禁止** C7→C0、邀请→人物、缺失→NARRATOR、冲突→UNKNOWN）→ `DirectorOutputValidator`（硬⇒REJECT / 软⇒DOWNGRADE 且表演字段退化安全默认值）→ `ScriptLineBuilder`（**text 恒为 canonical sourceSpan 切片**，运行时断言 fail-closed；speaker/type→TTS 路由；status=PENDING，**ACCEPTED_ALL ≠ COMMITTED**）。③ **第一批评估（用户指定四项指标）**。批次 A（M0 25 对白目标×3）：**硬失败 0%（修 prompt 前 96%/100%）、cosmetic 100%、ScriptLine 安全生成 100%、ACCEPTED_ALL 75/75**；批次 B（G_APP5 8 段含旁白）：**硬失败 0%（加 shortcut 前 62.5%）、cosmetic 30%、安全生成 100%、ACCEPTED_ALL 7 + DOWNGRADE 3、shortcut 7**，路由样例 `NARRATION/NARRATOR→AUDIO8` 与 `DIALOGUE/C0→COSYVOICE`。④ **评估暴露并修掉两个设计问题**：**(a) prompt 歧义** —— 片段行写成 `S0 [narration]`，模型把展示标签回填进 `type` 字段 ⇒ 96% SPEAKER_TYPE_CONFLICT（Validator 行为正确，是输入表达问题）；改为中文方括号 `〔旁白〕/〔对白〕`+告知已确定类型。**(b) 旁白段不该问模型** —— 旁白被塞候选后模型"提到谁就选谁"⇒ 15/24 冲突；`skipDecision()` 扩展为"旁白⇒NARRATOR 不问模型" + "空候选⇒UNKNOWN"，批次 B 硬失败 62.5%→0%。⑤ **已知缺口（非回归）**：两批次 UNKNOWN/VERIFY 均 0% —— 模型从不主动说 UNKNOWN（自 M0 起），当前仅由 shortcut 触发；需在 LoRA/prompt 给"证据不足"正反馈，否则长期"永远给一个角色"（假确定）。⑥ **测试：M3 全套 37 tests / 0 failures**（含 Normalizer 允许/禁止逐条、硬失败 4 类、软失败降级、UNKNOWN⇒VERIFY、路由、canonical 断言、shortcut）。证据：`runs/mobile_005_director_real/m3_step34_protocol_chain/report.md`、`m0_protocol_probe/m3_batch_metrics.txt`、`gapp5_batch/m3_batch_metrics.txt`、`data-room/.../semantic/{DirectorDecisionV2,ProtocolNormalizer,DirectorOutputValidator,ScriptLineBuilder}.kt`。
- **MOBILE-005 / M3 步骤 2 PASS：局部 ID 契约 + 共享协议渲染器（2026-09-17）**：① `CandidateSpeaker` 拆成 **`localId`（C0…，模型唯一可见）** 与 **`identityId`（不得进 prompt，仅供回填/审计）** —— 此前 `id` 直接把内部身份送进 prompt；② 候选编号与**每样本确定性 permutation 统一由 `ContextBuilder` 分配**（种子只依赖 `(paragraphId, segmentIndex)` ⇒ 桌面/设备一致、可复现），消费端（M0/M2）不再各写一份；③ `identityConstraints` 改用局部 ID（`C0 != C1`）；④ 候选新增 `sources`（CUE/RECENT_MENTION/SCENE_ACTIVE）供审计"正确角色是谁召回的"；⑤ 新增 **`DirectorSelectionPrompt`（共享渲染器，DIRECTOR_SELECTION_V1）**：规则只选 C#/NARRATOR/UNKNOWN、evidence 只填 E#、**不要复述原文**，明确 UNKNOWN 语义，证据段带 `[KIND]` 与**情绪参考 emotion_hint**，空候选+对白 ⇒ `skipDecision()` 直接 UNKNOWN 不调用模型；M0 探针改用同一渲染器（消除协议漂移）。⑥ 顺带修掉真实书暴露的两类新碎片：`曹伟却说道`→`曹伟却`（副词夹在名字与 cue 动词【之间】，`TRAILING_ADVERB` 补齐 却/则/便/即/更/已/尚/这才）与 `边跑边说道`→`边跑边`（`DeliveryCueLexicon` 新增 PARALLEL `边X边` 规则）。**结果：55 tests / 0 failures；Task070 G1–G12 未回归（G6 断言按 ADR-055 更新为"局部 ID + 身份可映射"，G12 冻结数据集输出**未变**）；G2 第五次对齐当前代码 PASS（desktop/device canonical md5 `fc9bfa4b…`，120 条），新 canonical 形如 `cands=[C0(e-林夜):林夜@1{RECENT_MENTION}] evidence=[E0:RULE:UNKNOWN]`；身份证据已在真实语料生效：`E0 曹健 ≠ 闫呢（DIALOGUE_BETWEEN）`**。证据：`runs/mobile_005_director_real/m3_step2_local_ids/report.md`、`data-room/.../semantic/DirectorSelectionPrompt.kt`、`m3/DirectorContextLocalIdTest.kt`。**★ 本轮还发现一个真问题：delivery-cue 判定与前后缀消歧都依赖 `knownEntities`，但真实管线构造 `RuleSpeakerBaseline()` 时没传实体集 ⇒ 上一轮加的消歧是死代码（单测全绿因为测试里传了实体集）**；已修为 `RuleSpeakerBaseline(store.canonicalNamesOf(bookPk))`（runner/fixture 同步；该调用被记录/回放覆盖，设备端同样生效）。同时修掉 fixture 的自洽循环（它用 cue 表面当实体集，把"对闫呢"残留也当实体）。规则层说话人从「对曹健｜指着甜妹｜对闫呢｜对堂弟｜闫呢忙｜曹健终于」收敛为「曹健｜甜妹｜吴颂金｜堂弟｜闫呢｜陈倩｜邀请」（仅"邀请"可疑，属"动词/角色名词当人名"另一类，记 TASK-100）。教训：**新增依赖上下文的规则后，必须核对真实调用点是否提供了该上下文**——否则新代码静默变死代码。
- **MOBILE-005 / M3 步骤 1b：采纳 v90.9 研究资产的三类判据（2026-09-17）**：用户提供 v90.9 原始工程（`多角色朗读2.85发音人轮询+增强别名检验v90.9.json`，783 KB JS / 4,078 发音人标签），按项目"逆向→自研重建"做法分析并**只采纳规则形态**（不搬代码）。① **群体称呼 ≠ 单人**：v90.9 用高精度形态（显式表 / 数量词+身份 / 成对描述 / 群体量词收尾）识别群体并**禁止与单人互并** —— 补上我们的真实缺口（`SegmentType.GROUP_SPEECH` 早已声明却从未赋值；`众人` 只在规则层 STOPLIST、候选编译器不设防）⇒ `NameEvidence.isGroupName/mergeBlockReason` 接入 `RuleSpeakerBaseline` 与 `SpeakerCandidateCompiler`；② **身份证据用有界文本模式**（不跨句、gap 有上限）：正向=别名提示词表+括注形式，反向=`A 不是 B`/`不是同一人`/`是两个人`/`A 只是 B 的⟨亲属⟩`（关系≠同一人）/`A 对 B 说话`（对话⇒不同人）⇒ `NameEvidence.aliasEvidence/contradictionEvidence` 进入 `ContextBuilder` 证据（`ALIAS_EVIDENCE`/`NEGATIVE_EVIDENCE`/`MERGE_BLOCK`），把原来的断言式 `"A != B"` 变成**有出处的证据**；③ **每类证据设预算**（v90.9 的 `ALIAS_RECENT_*` 纪律）⇒ 证据装配改为 DELIVERY_CUE ≤2 / 身份 ≤3 / RULE ≤1 / SPEAKER_CUE ≤1，总上限 7。**未采纳**：API 投票裁决（违背端侧使命，且 v90.7 基线已证其平票取最晚、`Math.random()` 兜底）、`reason` 自由文本的泛词扫描（按其自身教训只做字段-理由显式矛盾）、完整正反图谱子系统（我们已有 `hasHardNegative`/identity_evidence，先落文本层判据）；**发音人标签轮询**（全局计数+最少优先+硬锁不被覆盖）记入 Voice 分配里程碑待办。**结果：5 个 NameEvidence 测试（含"不得跨句/gap 有上限"反向保护）+ 44 个既有测试全绿（49 tests/0 failures）；G2 第四次对齐当前代码 PASS（desktop/device canonical md5 `0cfaf033…`，120 条）**。分析：`docs/baseline/v909/01_ALIAS_AND_SPEAKER_DELTA.md`（自写，不含第三方代码）；原始资产 `research/private/legado-v909/`（RESEARCH_ONLY）。
- **MOBILE-005 / M3 步骤 1 PASS：评价口径冻结 + cue 污染修复（2026-09-17）**：用户定案"parity 不再是最高目标，语义质量才是"，并指出最该先修的是 **Candidate/Gold 口径**。本轮：① **gold 分型**（`GoldPolicy`：MANUAL_GOLD 唯一可进硬准确率 Gate / WEAK_GOLD / RULE_BASELINE / NONE，`requireHardGate()` 对非 MANUAL_GOLD 抛错）——根因是**把规则层输出当成了 gold**，`饶有兴致道：` → 候选与"gold"同时被污染 ⇒ 会出现"模型答对/准确率 100%/产品荒谬"；② **cue 是证据不是 mention**：`DeliveryCueLexicon`（显式四字状语表 / 地·然 后缀 / 方式-情绪词根 / cue 动词自带）把 `饶有兴致/冷冷地/沉声道/不耐烦地` 判为表演提示 → `EvidenceItem(DELIVERY_CUE, emotion_hint)`，**不进候选、不当 speaker**；实体集安全阀保证不吃真人名；③ 顺带修掉**动词短语残留**（`闫妮追问道`→`闫妮追`、`曹健开玩笑道`→`曹健开玩`）：CUE_VERB 词表扩展 + **实体前缀消歧**（仅当整块非实体且其前缀是实体、残留≤2 字，无实体集时不启用）；④ **ContextBuilder 此前完全未用 TASK-095 的 Candidate Compiler**（仅靠窗口已有 speaker）⇒ 改为 `CUE ∪ RECENT_MENTION ∪ SCENE_ACTIVE`（新增只读 `canonicalNamesOf(bookPk)`），候选不再依赖规则层先给出 speaker；⑤ 目标选择不再要求"规则层给了 speaker"（否则最难的段被排除、UNKNOWN 永不出现）。**结果：实体发现 65→**59**（两轮共剔除 6 个碎片）；M2 语料 47 targets/storeCalls=0/候选多为假 → 120 targets/storeCalls=1289/候选=[C0:林夜@1]（RECENT_MENTION）；canonical 字段变为 `rule=- rule_status=UNKNOWN gold_kind=NONE`；G2 在新语义下重跑 device==desktop（canonical md5 `cb6db756…`）⇒ 仍 PASS；新增 8 测试 + 既有测试全绿（44 tests / 0 failures）；设备 M0 复跑：P1/P2/P3 25/25、P4 24/25（1 例格式越界 = ProtocolNormalizer 用例）、3 次逐字一致 25/25、Agreement with Rule Layer 47.4%（弱口径）、MANUAL_GOLD=0**。诚实记录：旧 M0 的"P5=76%"按新口径**不是准确率**，而是"与规则层一致率"（已改用修正 fixture 重跑）；规则层 recall 下降是**正确方向**（UNKNOWN 合法 > 编造假名）。裁决书 **ADR-055**。证据：`runs/mobile_005_director_real/m3_step1_candidate_gold/report.md`、`data-room/.../semantic/{DeliveryCue,GoldLabel}.kt`、`m3/DeliveryCueAndGoldPolicyTest.kt`、`tools/mobile005/{judge_selection.js,run_m2_device.ps1,run_m0_v2.ps1}`。
- **MOBILE-005 / M2 PASS：设备端语义层与桌面位级一致（2026-09-16/17）**：① 抽只读接口 `CharacterReadStore`（只抽语义层实际调用的 **9 个只读方法**，用调用面统计得到；`CharacterStore : CharacterReadStore` 仅加 override，零逻辑改动）=⇒ data-room semantic+character **13 测试 PASS**。② 新模块 **`semantic-core`**（Android 可消费视图，parser-core 同款 sourceSet 手法：srcDir 指向 data-room 源码 + exclude JDBC/IO），**桌面与设备共用同一份实现**；app-android 已依赖并编译通过。③ G2 机制：`RecordingCharacterStore`（桌面记录只读调用）+ `TraceCharacterStore`（设备按 key 回放，**缺 key fail-closed**）+ **`M2ParityRunner` 两端共用执行体** + `SemanticParityActivity`（设备 dump）。**结果：desktop 47 条 canonical md5 `36ea14e6ed63c8ebd842b019d75b5dcf` == device 47 条 `36ea14e6…` ⇒ G2 PASS（逐字节一致）**；判据面含 pid/si/type/sourceSpan/text/gold/候选 C#(名@距离,别名)/identity_constraints/embodiment/recent 窗口。桌面回放自校验 `TraceReplayParityTest` 亦 PASS（traceKeys=96，回放与 JDBC 路径逐字相同）。**方法论要点：device.jsonl(45,772 B) 与 desktop_semantic.jsonl(46,996 B) 字节不同（org.json 在 Android/JVM 键序不同）⇒ 判据必须是两端同函数生成的 canonical 文本，直接 diff JSON 会得到伪不一致。** ④ 两个坑：**禁止用 PowerShell 文本往返改构建脚本/UTF-8 源文件**（`Get-Content -Raw`→`Set-Content -Encoding utf8` 往返后 Gradle *静默只执行 plugins 段*：sourceSets/dependencies 全被忽略、连 throw 都不触发、jar 261 B、compileKotlin NO-SOURCE；用 write 工具重写后 37 class 正常）；`kotlin.include(...)` 在本工程不生效，新模块一律用 `kotlin.exclude(...)`。⑤ **带入 M3 的素材问题**：gold 里混入 cue 副词碎片（如 `饶有兴致道：` → gold=饶有兴致 且被当 CONFIRMED），属候选/规则层质量（TASK-100 领域），M3 的 Validator/候选 gate 需排除或降权，否则准确率指标无意义。证据：`runs/mobile_005_director_real/m2_semantic_parity/{report.md,trace.json,desktop_canonical.txt,device_canonical.txt,desktop_semantic.jsonl,device_semantic.jsonl}`、`data-room/.../{character/CharacterReadStore.kt,snapshot/*.kt}`、`semantic-core/**`、`tools/mobile005/semantic_parity.js`。
- **Road B P16-FIX：DSP RoPE 配对偏移 bug 修复，0:23 转正（2026-09-16）**：`htp-ops-lib/src/dsp/rope_ops.cc` 的旋转配对/写回偏移用了 `half_head_dim`(=head_dim/2=128) 而非 `rope_half_head_dim`(=rope_dim/2=32)，透传区写成 `[half_head_dim, head_dim)` 而非 `[rope_dim, head_dim)`（CPU 参考 `MNNRoPEComputeBasic` 为准）；顺带修掉 fuse 路径 trig 暂存跨 token 越界读。**同会话 A/B（CPU baseline 与旧 skel 均逐字节复现历史文件）**：L3 FusedRoPE 四分块 0.8714/0.9993/0.7678/0.9993 → 0.99984/0.99928/0.99944/0.99935，透传区逐位不变；端到端 500tok logits cos **0:23 0.956355→0.994986**（argmax 95826→124483）、12:23 0.990833→0.993881、7:7 0.981213→0.990193、0:7 0.971053→0.993291，0:0（无 RoPE 对照）逐字节不变；确定性两次逐字节相同；**24-token 生成文本 0:23 与 CPU 逐字一致**（修复前是"…到公元960年…"事实错）；skel A/B 18 样本计时无回归。**结论变更：默认配置由 `config_rd_cpu_greedy.json` 改为 `MNN_HEX_LAYER_HTP=0:23`（本次会话既最准又最快；0:23 vs cpu 的 margin 本次 2~12%，P15 会话 20%，绝对数字待安静设备复测）。** 边界：C4 未融合路径与其它 head_dim/rope_dim 形状只有索引级仿真证据。证据：`PLAN-20260916-057-dsp-rope-partner-offset-fix.md`、`runs/mobile_000b2_hexagon_la/m2-3-nodealign/report.md` P16-FIX 段、`p15/P16_FIX_SECTION.md`、ADR-050。
- **MOBILE-004 decode 路径剖析 + A/B/C 架构判定（2026-09-16）**：用户定案"先改 decode 执行方式，不动模型"，并要求对比 A（CPU 全程）/B（HTP prefill+CPU decode）/C（HTP 全程）。本轮产出：① 在 JNI 加**独立墙钟**（`wall backend=… decode_wall_us=…`）与 App 内 `MNN_OP_TIMING` 开关，墙钟与 MNN 计数逐轮吻合（<6%）；② **App 内 per-op 剖析（产品口径）**：每 decode token `cpu|Copy 46 ms（106 次跨边界/次 0.43 ms）+ lm_head(cpu|Convolution) 19 ms + cpu|LinearAttention 18 ms`，而 24 层 Dense 在 DSP 的 host 侧提交仅 **~0.3 ms/token** ⇒ 瓶颈是**跨边界搬运与 CPU 常驻 op，不是 DSP 算力、也不是提交次数**（修正我上一轮"提交开销是主因"的猜测）；③ **A/B/C 判定（含一次自我更正）**：CPU decode 实测在 **15 / 48 / 242 / 263 ms/token 之间抖 17 倍**（同进程相邻两轮即可从 15 跳到 48），HTP decode **稳定 60~72 ms/token**；我上一版据单次采样得出的"CPU decode 15.3 ms/tok ⇒ CPU 更快"是采样点运气（lesson ㉒ 再次应验）。⇒ **B 不是最优解**（它把瓶颈 decode 留在 CPU：最好与 A 持平、最坏与 A 一样糟），**C 是产品基线**（P95 稳定 5~6 s）；④ 下一轮三个已量化杠杆：lm_head 移出 CPU（19 ms/tok，A/B/C 通吃）、减少 decode 跨边界次数（46 ms/tok，让 LA 在 decode 上 HTP）、才轮到 layer-level 融合。证据：`runs/mobile_004_director_engine/decode/profile_20260916.md`、`decode/app_htp_optype.txt`、`decode/wall_*.stream.log`。
- **MOBILE-004 App HTP Gate（G_APP1–G_APP5，2026-09-16）**：**根因级发现**：app 原 `libMNN_htpops_skel.so`（1.94MB，来历不明）与它自己的 `libMNN.so` 不是同一 ABI 产物 —— 这才是 M2 时期 "HTP Branch A: lm_head 输入 cos=-0.0067" 的来源（不是算子问题）。本轮把 jniLibs 换成**同一构建树的匹配对 + 16KB 对齐**（libMNN `02f43bad…`、libllm `30302400…`、skel `4e38fd00…` 含 RoPE 修复、Express `19F6BAD8…`），并修 JNI 环境（`MNN_HEX_LAYER_HTP` 12:23→**0:23**、`LA_FORCE` 1→0、显式 `LA_OFF=1` 把 LinearAttention 钉在 CPU）、加显式实验开关 `readerdirector.backend=cpu|hexagon`（默认 cpu，走 QwenBackendPolicy/NPU_EXPERIMENTAL）、加 gate 通道（rawprompt/maxtokens/autorun + logits dump 到 files/）、APK 不再 strip skel。**Gate 结果**：**G_APP1 PASS**（load ok、无 crash；但 App 跑 423MB 模型会触发系统 LMK 杀其它 App）；**G_APP2 PASS（逐字节）** App HTP logits MD5 = shell HTP = `233FEE53…`，App CPU 亦 = shell CPU = `5C242819…`；**G_APP3 PASS**（药老 fixture JSON 与 golden 逐字一致）；**G_APP4 PASS**（App prefill HTP 1.15~1.32s vs CPU 1.53~1.81s，三轮交替全胜）；**G_APP5 FAIL**（真实书《我的脑力无限进化》第 1 章连续 5 段：格式 5/5 合格，但 ≥3/5 语义错——speaker 误判/文本漂移到相邻段落/type 误判；**CPU 对照复现同型错误 ⇒ 判定为模型+prompt 能力问题，非 Hexagon 回归**）。**关键副产物**：对真实 Director 请求 HTP **不是无条件更快** —— decode 占 78%（prefill 1.2s + decode 4.3s vs CPU 最好 1.5s 但争用下退化到 16.7s）；HTP 的价值是 prefill 快 + 稳定。★ 该条末尾"下一步应打 L=1 的提交/同步定长开销"已被下一条（decode 剖析）更正：提交只占 0.3 ms/token，真瓶颈是跨边界搬运与 CPU 常驻 op。证据：`runs/mobile_004_director_engine/app_htp_gate_20260916.md`、`golden_shell_htp_0_23.md`、`logs/**`、`gapp5/**`；备份 `_applibs_backup_20260916/`。
- **Road B P16-3：D2H 重排重写，0:23 prefill −11~41% 且 logits 逐字节不变（2026-09-16）**：先按**将发布的配置（0:23）**重测时间归因（新脚本 `p163_attrib.ps1`，logcat 边跑边流式抓）：`cpu|LinearAttention 857 ms(47%)` / `cpu|Copy 651 + htp|Copy 241 (49%)` / DSP 内部仅 ≈290 ms ⇒ **推翻 P15-2 的"Dense 54%"**（那是 12:23 下 CPU 跑 12 层的耗时；0:23 上 Dense 早已全在 DSP，host 侧仅 22 ms）。新增 `MNN_HEX_COPY_PROFILE_LOG` 画像（MNN 自带 copy 画像只在 `~HexagonRuntime()` 打印、llm_demo 退出不执行）把搬运拆成 flush/host/repack/convert：**repack 640 ms（D2H 620 ms、174 MB ⇒ 280 MB/s），convert 仅 0.3 ms**；根因是 `nc4hw4DeviceToHost`/`deviceToNchwHost` 写成「plane 外层 + 逐元素两次整除 + 每次调用堆分配 + 跨 8 KB 散写」，而同文件的 H2D 对称路径是「channel 外层 + NEON」快 18 倍。按 H2D 同构重写（构建前用 `p163_repack_sim.js` 做 14 组形状的逐元素等价仿真，**当场抓到第一版 channel 步长写错，未进构建**）。结果：**repack 640→61 ms（D2H 15.9x）、copy 合计 1016→449 ms、prefill A/B 12 样本 1.591→1.412 s（每轮配对均新优于旧，比值 1.11~1.69x，随负载变化）**；**0:23/12:23/7:7/0:0 四个配置 logits MD5 与优化前完全相同、24-token 文本一致 ⇒ 零数值变化**。修复后最大单项变为 `cpu|LinearAttention`（按设计留 CPU，DSP GDR prefill 更慢）。边界：本收益要在 **App 侧 HTP Gate 重跑通过**后才能进用户路径（ADR-050 明确不自动切换）。证据：`p15/P16_3_SECTION.md`、`report.md` P16-3 段、`p163/**`、libMNN.so md5 `edf6acde…`。（注：本条的 P16-2 归因工作在同一会话内先于 P16-3 完成。）
- **Road B P16-2：「Hexagon 0.99x 精度赤字」证伪 —— 是 fp16 参照系假象（2026-09-16）**：先做逐算子**新增误差**归因（`MNN_HEX_CHECKPOINT_IN=1` 落盘节点输入+输出，`added = cos_in - cos_out`，脚本 `p162_incr.js`）：**注意力/RoPE/归一化全部干净（≤1e-4），误差 100% 来自 W4 dense 投影**（L3 与 L0 两种层类型 sum(positive added) 都约 4.1e-3；同形状 1024→3584 的 `mlp/gate` 干净而 `mlp/up` 脏 10~20x ⇒ 逐张量的 W4 量化/舍入属性，不是形状或 kernel 分支）。再做决定性对照：新增 `config_rd_cpu_fp32.json`（同模型同权重，只把 `precision` low→normal，两边逐字节可复现）⇒ **`cos(CPU fp16, CPU fp32)=0.995184`，而 `cos(HTP 0:23, CPU fp32)=0.996345`**，即 Hexagon 0:23 比 fp16 CPU 基线**更接近 fp32**，argmax 也与 fp32 一致（fp16 CPU 的 95815 在 fp32 下是次优项）。结论：**Hexagon 与 fp16 CPU 的分歧就是 fp16 噪声地板，没有 Hexagon 专属缺陷可修；P10 冻结门限 0.997187 是 fp16 口径下的数，任何 fp16 实现都到不了**。反面证据（必须同时引用）：同一自然 prompt 的 24-token 生成，CPU fp32 反而给出史实错误（610 年），fp16 CPU 与 HTP 0:23 都给 618 ⇒ cos 与文本质量不单调，**不能声称 fp32 任务上更对**。附带负面记录：`model_fp16`(3GB) 在本机静默退出跑不起来；`model_sym` 属不同导出不可直接比优劣。方法论 lesson ㉓㉔ 已入报告。证据：`p15/P16_2_SECTION.md`、`report.md` P16-2 段、ADR-051。
- **四模型 Runtime 架构 v2（2026-08-24 用户定案，FROZEN）**：Qwen3.5-0.8B=导演（常驻）/ Qwen3-TTS VoiceDesign=选角（默认 Unloaded，新角色时才跑）/ CosyVoice=角色演员（常驻）/ Audio8=旁白（常驻）。**Voice Bridge**：VoiceSpec→3 候选取稳→canonical 文本 enrollment→VoiceProfile（`voices/<char>/` 永久资产）；音色=谁在说、情绪=怎么说，永不为情绪重造声。**SpeechScheduler**：Audio8/CosyVoice 各自串行队列（引擎间并行）、ReorderBuffer 有序重排、持久 AudioTrack（audio-marker 高亮/跳转/断点）；lookahead 30~100 段预造声；epoch 跳章 cancel。supersede 旧 §3.5/§4 "禁止 LLM+TTS 同驻"。文档：`VOICE_RUNTIME_ARCH.md §8`；参考：MnnLlmChat=LLM 层、MnnTaoAvatar=调度层第二 reference、MNN Hexagon=Road B 内核。
- **Audio8 codec MNN-QNN/HTP（2026-08-24，独立 Gate）**：高质量、非蒸馏 T16 teacher 已经通过 MNN 3.6.1 offline QNN plugin 在 BKQ-AN90/SM8850/V81 的真机执行。4个真实case对ONNX最差PCM cosine=`0.999098`、SI-SDR=`27.43dB`；50/200次热态RTF=`0.320854/0.321114`，达到用户约0.5目标。旧direct `qnn-net-run`约1.3–1.57主要是runner/integration口径；低质量FT2 student不再是速度必需路线。边界：当前只完成codec token→PCM，完整text→多帧AR→PCM、系统级内存/功耗、ReaderVoice共同runtime仍OPEN。证据：`E:/AndroidStudioProjects/audio8tts-mnn/docs/reports/AUDIO8_MNN_QNN_HTP_20260824.md`、ADR-049。

- **VoiceIdentity Source 解耦：enrollment 算法唯一化 + 采样参数运行时化（2026-09-17，ADR-054）**：① `CosyVoiceEnrollmentCore` 成为 enrollment 的**唯一实现**（`CosyVoiceEnrollmentJni.cpp` 410→54 行薄包装），「参考音频克隆」与「文字设计音色」共用同一条链。② VoiceDesign decoder 出 PCM 后**直接** `enrollFromPcm`，不再经 WAV 往返；`reference.wav` 仍写，定位改为**长期资产**。③ PCM 入口第一版显式保持 int16-WAV 数值语义（内存内 round-trip），**不追求更高精度**。④ **Gate E1（SHA256 bit-exact）PASS**：同 WAV 下 LEGACY `.so` 与新 Core 的三个产物（`prompt-speech-tokens.csv` / `prompt-cond.bin` / `spks.bin`）**逐位相同**。⑤ **全链 E2E PASS**：`design-loop.log` 返回串里出现 `ENROLL_OK tokens=125 frames=250 ms=18158`（native 内部完成注册），音色目录含全部产物 + `source.wav` + `design.json`，用该音色合成 `6.40 秒 · peak 0.898 · rms 0.156 · finite=True`。⑥ **采样参数修复 PASS**：MNN-LLM 的 `top_k/top_p/repetition_penalty` 默认值是"全关"，导致同一句话生成长度 3.04/10.48/20.00 秒（3.4 倍波动）；写入官方值（`top_k=25 top_p=0.8 repetition_penalty=1.1`）后稳定为 **2.20 / 2.20 秒**。`llm_config.json` 因此**移出 `MODEL_FILE_SPECS`**，改由 `ensureLlmSamplerConfig()` 运行时合并。

## 已失败路线（禁止重走，除非新证据）

- 整 LLM NPU：Token 坍缩（CosyVoice 主链），不作为正式路径。
- HiFT QNN/HTP：速度快但最终 PCM 未过质量门。
- Flow 原始 2-step 蒸馏输出：RMS +5.36dB / speaker cosine 0.916 被拒（student held-out mel cosine 0.99882 / SNR 26.26dB 为可用产物）。
- mnn-model-llm-w4（W4 block64 LLM）：weight 文件 0 字节，未完成，不可部署。
- **VoiceDesign Decoder 提速（2026-09-16，ADR-053）**：五条路线全部实测失败，禁止无新证据重走 —— ① 同 RuntimeManager 内分阶段 Module 释放：anon 仅降 56 MB（575,448→518,196 kB），峰值 HWM 反升到 1.93 GB。② FP16 Decoder：文件减半（457→228 MB）但 int16 逐样本最大差 **2693 LSB**。③ Decoder T 300→96：内存与磁盘均无收益（差 0.018%）。④ `RuntimeManager::setCache`（OpenCL kernel 缓存）：卡在 decoder 后不再推进。⑤ `USE_CACHED_MMAP`+EXTERNAL_WEIGHT_DIR：写到 1 GB 卡死，且方向与省内存相悖。**当前唯一稳定配置 = Talker 与 Decoder 均外置权重**（anon 0.61 GB / HWM 0.90 GB / Decoder 459 s，已跑通 2 次）。证据：`docs/experiments/VD-DEVICE-MEMORY-20260916.md`。
- **GraphB 权重存储格式 / 运行时驻留 / 部署边界（2026-09-17，ADR-053 增补）**：**Storage compression != Runtime-memory compression。** GraphB 的 FP16 external weight 是 2.82 GB，weight-only INT4 把它压到 884 MB（磁盘 3.19×），但**运行时 `RssAnon` 基本不变（fp16 3.53 GB / int4 3.52 GB）** —— 在当前 MNN 3.6.1、当前 GraphB 转换方式和 CPU backend 路径下，量化权重会被展开为执行侧表示。**判据必须是运行时 anon，不是文件大小。** 另实测：① `USE_CACHED_MMAP=2` + `MMAP_FILE_SIZE=4096` 能把 anon 压到 0.27 GB ✓ 但 MNN 预分配正好 4 GB 的 `.static` 后 **prefill 死等**（0 jiffies）✗；② ONNX 内嵌物理不可能（2.82 GB > 2 GB protobuf 上限）✗；③ `--weightQuantBits 8` 未生效（`.weight` 字节数不变）✗。**未试**：拆成多个 <2 GB 的内嵌 `.mnn`（应先做 `GB-SPLIT-P0` 4L A/B）、蒸馏更小的 GraphB（应独立立项 "Small VoiceDesign"，不作部署优化的尾巴）。另：external weight 的代价是**双重的**（既慢又占 anon）；`_RebuildExternalOp` 把 2.82 GB 读进 anon，且**与 backend 无关**（cpu/opencl 只差 76 kB），96% 落在 `[anon:scudo]`（native malloc，与 Kotlin 无关），**不是泄漏**（41→83 帧 anon 稳定在 4.016 GB）。证据：ADR-053 增补节、`rss.log` 逐点打点。

## 下一正式任务

- **TASK-070 SemanticSegment + Speaker Assignment + NarrationIR（先 Rule-only baseline）**：输入 = LogicalParagraphRevision + CharacterObservation 流 + CharacterRevision snapshot；输出 = NarrationIR v1。

## 待核验事实（Open Facts）

```text
PERF-010-01:
Current importer is functionally frozen (TASK-010 PASS),
but readAllBytes is a desktop/JVM reference path (3.3M chars → ~269MB JVM heap).
Before Android integration, benchmark and migrate to streaming/channel/mmap
ingestion if required. 不阻塞 TASK-020（直接建立在 immutable source/index 上）。
```

```text
PERF-030-01:
LayoutProfiler: 166,009 lines → 39.6 s
Status: FUNCTIONALLY_ACCEPTED / PERFORMANCE_DEBT
Reason: boundary+paragraph build only ~1.35 s，主要热点明确集中在 Profile。
TASK-040 不顺手优化，后续单独做 profiler incremental/window cache。
```

```text
GEN-030-01:
Engineering regression = PASS
Real-book generalization = OPEN
Required: 20–30 real TXT books（TASK-020/030 均不伪称泛化准确率）
真实书样本（TASK-040 起）：PRIVATE_REALBOOK_001 = 《娱乐：从1990年开始》（作者 咖啡香草），
2.97MB / 36,948 行 / 463 章 / 35,969 段；gitignored（版权不入库）
```

```text
PERF-040-01: DB storage debt — 3.3M 字书 ≈ 322MB DB（~810k 行，~415B/行）→ OPEN
PERF-040-02: JVM heap 1.59GB —— 已由 PRE-TASK060 PERF REVIEW 重审：
  编译器生命周期峰值仅 ~124MB，S5 置空+GC 后 6MB，persistence 基线 6MB；
  1.59GB 为旧测量方法（编译器集合+批量+未回收垃圾同驻留）的瞬时叠加，非架构问题。
  PRE-060 结论：compiler lifetime 可控（PERF060-P1/P2/P3 ✅）；DB 存储债务独立 OPEN（PERF-040-01）。
  PERF060-P4（Character evidence 规模估算）在 TASK-060 schema v2 前完成；P5 Android gate OPEN。
PRE-TASK060 PERF REVIEW REQUIRED 状态：已完成（docs/experiments/PRE_TASK060_PERF_REVIEW.md）
```


| # | 事实 | 状态 |
|---|---|---|
| F1 | TTS 常驻内存口径：947MB（VmRSS） vs 2.25GB（PSS 假设） | 不一致，真机重测后冻结 |
| F2 | JNI 导出符号 vs `Java_io_legado_app_cosy_*` 声明 | 重编 so 前 nm/dumpbin 验证 |
| F3 | Conditioner 子进程启动开销 | 真机 profiling |
| F4 | v90.7 硬编码智谱 key 是否仍有效 | 建议作者轮换；ReaderVoice 不复用 |
| F5 | hutao voice profile schemaVersion 1 vs VoiceProfileRevision | 设计对齐时验证 |
| F6 | W4 LLM 是否值得重做（8GB 预算） | 8GB 模式设计时决定 |
| F7 | `v2.48.40.260702/qairt/` 内容 | 未深入 |
| F8 | v90.7 授权确认（作者许可） | 待联系作者 |


